## 2026-08-30T18:50:56Z — retro v0.1.0 — process-analyst

- **Item:** EP-001
- **Trigger:** no status made this runnable — `retro` is invoked directly. It was invoked on a banked copy of this engagement's record, whose `RECORD-NOTICE.md` states that the product source tree and the commit history were not banked with it. `scripts/engagement-state EP-001` reports `ended`, and no `artifacts/retro.md` existed.
- **Inputs read:** the same list as `## What was read` in the report.
  - `tracker/items/EP-001/item.md`, `history.md` (10 rows) and `journal.md` (14 entries), and all six questions on the epic including the three `kind: sign-off`
  - `tracker/items/WI-0001/`, `WI-0002/`, `WI-0003/`, `WI-0004/`, `BUG-0001/` — `item.md`, `history.md` and `journal.md` for each, in full: 60 more history rows and 63 more journal entries, 77 entries in total, read entry by entry and not by heading
  - all fourteen questions on the children, read as exchanges: who asked, who was asked, what was offered, what came back, what changed, and how long each took
  - all 25 artifacts — five `plan.md`, five `impl-report.md`, five `verify-report.md`, five `review.md`, four `refinement-qa.md` and the epic's termination review; the five `review.md` and two `verify-report.md` in full, the rest by section against the entries that produced them
  - `docs/product/vision.md` (v11) and `docs/architecture/overview.md` (v9) in full including both change logs, and ADR-0001 through ADR-0010
  - `tracker/project.yaml`, `tracker/board.md`, `tracker/requests/` (empty)
  - the installed contracts and `skill.yaml` of `intake`, `refine`, `plan`, `implement`, `verify`, `review-close` and `answer-questions`, plus `.claude/agile-skills/VERSION`; `spec/retro.md`, `spec/journal-and-history.md`, `spec/dor-dod.md`, `spec/question.md`, `spec/doc-header.md`, `spec/workspace-layout.md`; and `scripts/lint-retro`, `scripts/lib/claims.py`, `scripts/lib/scope.py`, `scripts/lib/textio.py`, `scripts/transition`, `scripts/journal-entry`
  - **Not available:** `mdtab/` and `tests/` (the product source), the commit history, and `IDEA.md`, `SIMULATION-NOTICE.md` and `HARNESS-STATUS.md`, all three cited by executions. Also unavailable: the contract of every version that acted — each entry names a version earlier than the one installed here — so the procedure's third reading, comparing an entry's gates with its acting contract, could not be made for any execution.
- **Decisions:**
  - **Twelve proposals, ten of them `toolkit-defect`, and the proportion is stated in the report rather than hidden.** The counterfactual test was written out for each and each can be stated without naming markdown, tables or alignment. The engagement's own executions had already identified most of them as toolkit observations and recorded that no item in a consumer project can file one, which is what the report is for.
  - **Two candidates were deliberately not proposed, because the toolkit installed over this record already addresses them.** The vacuous `lint-claims --changed-since` window — recorded by executions on four items — is now modelled as a third, failing state in `scripts/lib/scope.py`, and `validate-workspace`'s `UnicodeDecodeError` crash is handled in `scripts/lib/textio.py`. Proposing a fixed defect wastes the channel the report exists to keep readable, so both are in `## Positive record` instead. The residue that is *not* fixed — that the gate is scoped to documents the change touched rather than to documents it falsified — is P-3.
  - **The hardest call was P-11 versus P-3.** Three sign-off rounds and two scope growths could be phrased as a defect in the sign-off protocol. The counterfactual could not be written without this stakeholder: an engagement whose stakeholder does not add a condition at the second acceptance never reaches it. So it is `project-circumstance`. What *does* generalise is separated out as P-2 — the ordering that lets a DE1–DE6 failure invalidate an acceptance already given — whose counterfactual needs no stakeholder at all.
  - **P-12 is an `observation`, not a defect, because this copy cannot decide it.** No question carries a `## Cross-answer check` and no entry records `cross-answer-consistency`, both of which the installed contracts require; but every acting version predates those contracts and they were not banked. `RECORD-NOTICE.md` says to name the versions and treat the difference as unknown, and that is what the entry does.
  - **P-4 and P-7 were kept apart although both are about a check that did not check.** P-4 is a human audit passing on an example that could not fail; P-7 is a machine's verdict and a worker's sentence not being reconciled. They have different fixes, and merging them would have produced one finding nobody could act on.
  - **P-10 was kept as a `toolkit-defect` at `low` severity rather than demoted to an observation.** Its counterfactual is general — any second item citing a first item's criterion by number — and the record shows it actually producing two stale citations. Severity is low because `refine` caught it by reading and the cost was minutes.
  - **Nothing was written about whether mdtab is any good.** That was `verify`'s question and `review-close`'s, it was answered before the ending, and the source tree is not in this copy in any case.
- **Questions raised:** none. This skill files none: the engagement has ended, and everything it would ask is in the report as an observation or a proposal.
- **Commands:**
  - `.claude/agile-skills/scripts/engagement-state EP-001` → exit 0, `EP-001 ended`, rest reached 2026-08-29T08:20:20Z
  - `.claude/agile-skills/scripts/validate-workspace` → exit 1, 13 errors, every one `claim.citation.unresolved` on a `mdtab/*.py` or `tests/*.py` path; re-run after writing the report with the identical 13 errors and no new one
  - `.claude/agile-skills/scripts/lint-retro EP-001` → exit 0, 0 errors, 0 warnings
  - `.claude/agile-skills/scripts/lint-retro EP-001 --require-scope` → exit 0, 0 errors, 0 warnings
  - `.claude/agile-skills/scripts/lint-claims --all` → 13 errors, the same 13; run to see whether the ADR-0003 false positive the record reported still reproduces — it does not
  - a script over all six `journal.md` files extracting every entry's heading and `**Gates:**` bullet, to compare each skill's gate list against its own other executions
  - a script over all twenty question files tabulating `created`, `answered-at`, `addressed-to`, `blocking` and `kind`
  - `grep -rn "forced\|override" tracker/items/*/history.md` → no gate was forced anywhere in the engagement
- **Gates:**
  - `engagement-has-ended` → **pass** (`scripts/engagement-state EP-001`, exit 0, verdict `ended`: the epic is `done` and no retrospective had been written)
  - `retro-report-is-well-formed` → **pass** (`scripts/lint-retro EP-001`, exit 0)
  - `scope-was-not-degenerate` → **pass** (`scripts/lint-retro EP-001 --require-scope`, exit 0: all six items named, the counts declared and within what the workspace holds — 6 items, 77 entries, 12 proposals)
  - `the-record-was-not-touched` → **pass**. This execution wrote exactly two files: `tracker/items/EP-001/artifacts/retro.md` and this entry on `tracker/items/EP-001/journal.md`. No item, question, artifact, document or history row was edited; no status moved; no board regenerated; nothing was filed.
  - `workspace-valid` → **fail**, and recorded as failed rather than passed. `scripts/validate-workspace` exits 1 with 13 errors, every one `claim.citation.unresolved` naming a path under `mdtab/` or `tests/` — the product source that `RECORD-NOTICE.md` says was not banked with this copy. It is a property of the copy and not a finding about the engagement, so per that notice the reading continued. The identical 13 errors, and no new one, are reported after this execution's writes.
- **Artifacts:**
  - `tracker/items/EP-001/artifacts/retro.md` (new)
- **Status:** `done` → `done` (unchanged)
- **Result:** The record of EP-001 is read and reported: 77 journal entries, 70 history rows, 20 questions, 25 artifacts and 12 documents. Twelve observations, eight entries in the positive record, and twelve proposals for triage — ten `toolkit-defect`, one `project-circumstance`, one `observation`. Two further candidates were left out because the installed toolkit already fixes them. Nothing in the engagement was written except this entry and the report.
