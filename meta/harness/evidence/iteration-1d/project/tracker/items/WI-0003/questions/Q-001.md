---
id: Q-001
item: WI-0003
from-skill: refine
addressed-to: human
blocking: true
status: answered
created: 2026-08-22T02:42:09Z
answered-at: 2026-08-22T02:52:26Z
answered-by: human
---

## Context

This question supersedes the first half of `EP-001/Q-002`, per `spec/question.md` §3 rule 6 — a
question is never reopened, it is superseded by a new one. `EP-001/Q-002` asked you for a sample
of your bank's CSV and for the rule by which a bank row becomes a shared expense. You replied:

> I'll send you a sample later.

That was recorded as answered because it is what you said and it had a real consequence — WI-0003
went last and nothing downstream proceeded as if the shape were known — but the sample has not
arrived, and `WI-0003/item.md` `## Notes` records, in writing, that `refine` must file this
question rather than attempt the item. So here it is. The second half of `EP-001/Q-002` — how a
row becomes a *shared* expense — is `Q-002` on this item, filed at the same time so you can answer
both in one sitting.

This is a **missing fact**, not a choice between options. Banks disagree on everything, and a
parser written against a guess fails on your first real file. What is already fixed, and what the
sample has to be reconciled against:

- an amount is a plain number with at most two decimals, no currency symbol and no thousands
  separator (`WI-0001` AC6, `ADR-0002`). If your export writes `€12,50`, `1,234.56`, `(42.50)` or
  a trailing minus, **the importer has to normalise it** — `ADR-0002` deliberately refused to
  widen what the hand-entry command accepts in order to make the import easier;
- an expense carries a date written `YYYY-MM-DD` (`WI-0001` AC7), and the import must carry each
  row's own date onto the expense it creates rather than stamping today (`WI-0001/Q-003`);
- a description cannot be empty (`WI-0001` AC3), so something in each row has to become one.

Until I can see a real header row and a few real lines, I cannot write a single acceptance
criterion for this item that anyone could test.

## Question

Please paste 3–5 real lines from your bank's CSV export, **including the header row if there is
one**. Change the merchant names and the amounts to anything you like — I need the shape, not
your spending.

## Options considered

This is not a choice between options; it is a fact only you have. `spec/question.md` §2 requires
that to be said explicitly rather than dressed up as a choice, so: there are no options.

What I would otherwise have to guess, and will not:

- whether there is a header row, and what the columns are called and in what order;
- comma, semicolon or tab as the delimiter, and how quoting works when a merchant name contains
  the delimiter;
- the date format — `31/01/2026`, `2026-01-31`, `31 Jan 2026`, or something else;
- how money out is written — `-42.50`, `42.50` in a separate debit column, `(42.50)`, or a
  trailing minus — and whether there is a currency symbol or a thousands separator;
- which column should become the expense's description, when there are usually several
  candidates (merchant, reference, memo);
- whether the file has a preamble or a trailing total row that is not a transaction.

If it is genuinely easier for you to attach the file than to paste from it, say where it is and I
will read it from there.

## Answer

[human] I'll send you a sample later.

## Consequences

The reply is a **deferral, not the fact**, and it is the second one — `EP-001/Q-002` got the same
sentence. It is recorded as answered because it is what the stakeholder said and it has a real
consequence, which is that this item still cannot be refined. Nothing about the file's shape has
been decided or guessed.

- `tracker/items/WI-0003/item.md` — `## Notes` rewritten: the two missing facts are now listed as
  one still missing (this one, deferred twice) and one settled (`Q-002`); AC1 states in the
  criterion itself that the column mapping waits on this question; a new
  `### Q-002 answered, Q-001 deferred a second time` section records why the item returns to
  `draft` rather than to `blocked`, and why no third request for the sample was filed now
- `tracker/items/WI-0003/artifacts/refinement-qa.md` — the deferral recorded verbatim as a
  `[human]` line, the round-1 table updated, and a closing section on where the item stands
- `tracker/items/WI-0003/history.md`, `tracker/items/WI-0003/journal.md` — the item returned to
  `draft`, its recorded `resume-to`

**Not** consequences, deliberately: no acceptance criterion about parsing was written, no CSV
dialect was assumed, and no Definition of Ready verdict was reached. The next `refine` execution
dispatched on this item must file a fresh question citing this one, per `spec/question.md` §3
rule 6, and suspend again if the sample is still missing.
