# Journal — EP-001

Append-only. One entry per skill execution, per spec/journal-and-history.md section 2.

## 2026-09-11T02:00:59Z — intake v0.5.2 — product-analyst

- **Item:** EP-001
- **Trigger:** invoked directly on the stakeholder's stated idea; `intake` is not dispatched on a status
- **Inputs read:**
  - `IDEA.md` — the stakeholder's statement, verbatim: *"A command-line tool for envelope budgeting: I set up named envelopes, put income into them, record spending against an envelope, and see what's left in each. I also want a simple monthly summary I can look at. Data must survive between runs. Python, no services."*
  - `SIMULATION-NOTICE.md` — the stakeholder answers asynchronously, in files, and is not in this session
  - `tracker/project.yaml` — name `envel-2`, trunk `main`, all three commands null
  - `tracker/items/` — no items existed; `EP-001` and `WI-0001` were the next free IDs
  - `docs/product/vision.md` — did not exist, so this execution created it; no prior `## Engagement state` section to leave alone
- **Decisions:**
  - **Restated the idea before creating anything, and put the restatement in front of the stakeholder rather than assuming it.** The stakeholder is asynchronous and could not confirm it in session, so the restatement opens the `## Context` of all six questions and `EP-001/Q-001` names itself as the place to correct it. Recording a confirmed understanding nobody confirmed would have been the one claim this record cannot support.
  - **Split into three items along the flow of money, not along the technical layers.** `WI-0001` is money in (create envelopes, fund them, read what is in each), `WI-0002` is money out (record a spend, see what is left), `WI-0003` is the look-back (a month's summary). Each delivers something a person can do on its own, and the order is forced by dependency rather than chosen: nothing can be spent from envelopes that do not exist, and a month with nothing recorded in it has nothing to summarise.
  - **Persistence is not its own item.** "Data must survive between runs" is not something a person can do; it is a property of the other three. It is written as a criterion inside `WI-0001` (AC4) and `WI-0002` (AC4) instead, where it can actually be observed by running the tool twice.
  - **Two criteria are held deliberately undecidable, and said so.** `WI-0002` AC5 (what an overspend does) and `WI-0003` AC5/AC6 (which month, and what the summary shows) are placeholders naming the question that decides them. Writing a plausible answer into them would have manufactured a requirement the stakeholder never stated, and it would have been indistinguishable from one they did.
  - **Ordering was read off the stakeholder's own sentence and off dependency, not guessed.** They named the capabilities in the order set up → put in → spend → look, and that is also the only order in which the items can be built. `WI-0001` and `WI-0002` are `high`, `WI-0003` `medium`. No question was filed about ordering, because there was nothing left to choose.
  - **Who this is for, and what they do today, is recorded as unknown.** The stakeholder was not present to be asked. `EP-001` `## Notes` and `docs/product/vision.md` both say it is unknown rather than filling in a plausible persona, and `EP-001/Q-001` invites it.
  - **Out of scope was written although the stakeholder named no exclusions**, derived from what a reader would otherwise reasonably assume: multi-user, any network or service, bank and CSV import, forecasting, recurring transactions, multiple currencies, a graphical or full-screen interface, periods other than the month, and encryption of the stored data.
  - **Removed the solution language intake had reached for.** An earlier draft of the items named concrete commands (`envel new`, `envel fund`, `envel spend`). The stakeholder said "a command-line tool ... Python, no services" and named no command, so the criteria now say "there is a command that ...", the command's name is asked as `EP-001/Q-006`, and its surface is left to `refine` and `plan`. Storage format and file location are named nowhere: they are design, and `WI-0001` `## Out of scope` says so.
- **Questions raised:** `EP-001/Q-001` (elicitation, non-blocking, to human — what else matters that nobody asked about), `EP-001/Q-002` (blocking — what an overspend does), `EP-001/Q-003` (blocking — what the monthly summary must show), `EP-001/Q-004` (blocking — which month, and how it is chosen), `EP-001/Q-005` (blocking — whether corrections and moving money between envelopes are in this first version), `EP-001/Q-006` (blocking — what the command is called). All six are open and all are addressed to the human; none has been answered, because the stakeholder is asynchronous and was not in this session. They were filed as one batch and share one opening frame so that they read as one ask.
- **Commands:**
  - `.claude/agile-skills/scripts/workspace-init .` → exit 0, workspace already present
  - `.claude/agile-skills/scripts/new-item --next-id epic` → `EP-001`; `--next-id work-item` → `WI-0001`
  - `.claude/agile-skills/scripts/new-item --id EP-001 --type epic ...` → exit 0; the same for `WI-0001`, `WI-0002`, `WI-0003` at `draft`
  - `.claude/agile-skills/scripts/lint-answers --item EP-001 --require-elicitation` → exit 0, 0 errors
  - `.claude/agile-skills/scripts/lint-documents --rule engagement-state-is-delimited --document docs/product/vision.md` → exit 0, 1 section, shape only
  - `.claude/agile-skills/scripts/board-gen .` → exit 0, wrote `tracker/board.md`
  - `.claude/agile-skills/scripts/validate-workspace .` → exit 1 on an intermediate run (an unterminated string in `tracker/project.yaml`, and journal entries not yet written); the description was rewritten as a folded scalar and the entries written, and the run at the end of this transition is the one this gate is recorded against
- **Gates:**
  - `workspace-valid` → **fail** (`validate-workspace` run by this transition against the state the move produces; the run reported FAIL: `/usr/bin/python3 /home/msi/agile-skills-throwaway/envel-2/.claude/agile-skills/scripts/validate-workspace --root /home/msi/agile-skills-throwaway/envel-2 --resolving 'EP-001:open->awaiting-answer+journal'` exited 1)
  - `epic-has-success-measures` → **pass** (manual check: each entry of `EP-001` `## Success measures` names something a person can carry out and observe — run the four steps in one session, quit between them and compare, check the month's figures reconcile arithmetically, observe that no other process was started, and try a spend against an envelope that does not exist. None of them restates the goal)
  - `an-open-question-was-asked` → **pass** (`lint-answers --item EP-001 --require-elicitation` → exit 0; `EP-001/Q-001` is the elicitation, open with the stakeholder)
  - `engagement-state-is-delimited` → **pass** (`lint-documents --rule engagement-state-is-delimited --document docs/product/vision.md` → exit 0; `docs/product/vision.md` carries exactly one `## Engagement state` section, and the rule checks shape only)
  - `items-are-separable` → **pass** (build order `WI-0001` → `WI-0002` → `WI-0003`, each depending only on its predecessor, each delivering something observable alone; stated under **Decisions:**)
  - `no-solution-in-the-problem` → **pass** (the invented command names were removed and what was removed is recorded under **Decisions:**; what remains — "a command-line tool", "Python", "no services" — is the stakeholder's own vocabulary)
- **Artifacts:**
  - `tracker/items/EP-001/item.md` (new), `tracker/items/WI-0001/item.md`, `tracker/items/WI-0002/item.md`, `tracker/items/WI-0003/item.md` (new)
  - `tracker/items/EP-001/questions/Q-001.md` … `Q-006.md` (new)
  - `docs/product/vision.md` (new, v1)
  - `tracker/project.yaml` (description filled in; `commands.*` left null, because this project has none yet and inventing one would make a gate report a pass for a command nobody can run)
  - `tracker/board.md` (regenerated)
- **Status:** `open` → `awaiting-answer`
- **Result:** `EP-001` is open with three draft items and a vision. Six questions went to the stakeholder as one batch; five of them are blocking, so the epic is suspended at `awaiting-answer` and will resume at `open` when they are answered. Nothing is designed and nothing is built, which is what this step is for.

## 2026-09-11T02:18:21Z — answer-questions v0.6.3 — architect

- **Item:** EP-001
- **Trigger:** `awaiting-answer` with six questions carrying replies; the stakeholder answered all six between turns, so they are answerable rather than outstanding (`spec/question.md` §3 rule 4b)
- **Inputs read:**
  - `tracker/items/EP-001/questions/Q-001.md` … `Q-006.md` — all six, each with the stakeholder's `## Answer` filled in
  - `tracker/items/EP-001/item.md` and `tracker/items/EP-001/history.md` — the epic, and the row that suspended it, whose `resume-to` is `open`
  - `tracker/items/EP-001/journal.md` — `intake`'s entry, for what was already decided and what was deliberately left open
  - `tracker/items/WI-0001/item.md`, `WI-0002/item.md`, `WI-0003/item.md` — the criteria these answers land in; `WI-0002` and `WI-0003` carried placeholder criteria naming these very questions
  - `tracker/items/WI-0001/artifacts/refinement-qa.md`, `tracker/items/WI-0002/artifacts/refinement-qa.md` — `refine` round 1, to check that nothing the stakeholder said here contradicts what it recorded
  - `docs/product/vision.md` v1 — the document these answers had to reach
  - `docs/architecture/adr/` — empty; no ADR exists yet, so no answer here could contradict one
  - `.claude/agile-skills/spec/question.md` §2 (`## Cross-answer check`), §3, §4; `spec/doc-header.md` §3, §4a; `spec/ids-and-statuses.md` §5
- **Decisions:**
  - **`Q-001` (elicitation) — the reading was confirmed and a rule we did not have arrived with it.** Route: the human answered. They confirmed intake's restatement, named themselves as the only user and a spreadsheet with a tab per month as what they do today, and then volunteered the thing they said they would least want us to get wrong: *"money left in an envelope at the end of a month stays in that envelope; rolling over is the whole point of doing it this way."* That is a product rule nobody had written down. It became a success measure on the epic, a paragraph in the vision, and `WI-0003` AC3 with a worked example — the non-blocking elicitation turned out to carry the most consequential sentence in the round, which is the argument for filing it at all.
  - **`Q-002` — an overspend is refused.** Route: the human answered, option A. `WI-0002` AC5 was a placeholder held open for exactly this and is now the rule: nothing recorded, amount unchanged, the message naming the envelope and what is left. They ruled out the recommendation intake had offered (option C, allow and warn) and gave their reason, which is recorded in `WI-0002` `## Notes` in their own words rather than paraphrased.
  - **`Q-003` — the summary is three columns, one row per envelope.** Route: the human answered, option A, and explicitly against intake's recommendation of option B. `WI-0003` AC2 is now their three figures and AC6 is now the exclusion they stated — no total line, no individual spends — rather than the placeholder it was. The exclusions also went into `## Out of scope`, because an exclusion stated only as a criterion is invisible to whoever next proposes the feature.
  - **`Q-004` — calendar months, the current one by default, an earlier one on request.** Route: the human answered, option A. `WI-0003` AC5 replaced.
  - **`Q-005` — the scope widened, and it widened by two items rather than one.** Route: the human answered, option C, against the recommendation of B. Step 3b applies: work an answer implies and no item records is filed as a `work-item` at `draft` with `arose-from`. `WI-0004` (move money between envelopes) and `WI-0005` (correct a recorded spend) are that. Two items and not one, because they have different criteria and different unknowns: `WI-0004`'s open points are about the summary and about symmetry with a spend, `WI-0005`'s is that nothing in this tool currently gives a way to refer to one recorded spend — `Q-003` removed the only report that might have shown one. Folding either into `WI-0001` or `WI-0002` is the move step 3b refuses, because it hides the scope change from the board.
  - **`Q-006` — the command is `envel`.** Route: the human answered, option A. Propagated into every acceptance criterion that said "a command", across five items, and into the vision.
  - **Neither new item carries `depends-on` yet, and that is deliberate.** `WI-0004` needs `WI-0001` and `WI-0005` needs `WI-0002`, and both dependencies are written in `## Notes`. Recording them in the frontmatter now would freeze each item's **refinement** behind the other item's delivery, because `pipeline.yaml`'s `runnable` rule gates every dispatch on `depends-on` being `done` — the same toolkit behaviour turn 2 recorded on `WI-0002`. `refine` should be free to put its questions to the stakeholder first; it can record the dependency when it has.
  - **No ADR was written.** Every one of the six answers came from the stakeholder in their own words, so the route was "the human answered the escalation" and the record is the `## Answer` itself. Nothing here was decided by the architect, and an ADR recording a decision nobody took would be the appearance of one. The two scope decisions this execution *did* take — two items rather than one, and no `depends-on` yet — are structural rather than architectural and are recorded above and in each item's `## Notes`.
- **Cross-answer check:** `EP-001/Q-001` checked against `EP-001/Q-002` (compatible), `EP-001/Q-003` (compatible, and it fixes what that answer's "what is left" column means), `EP-001/Q-004` (compatible — it fixes the boundary carry-over happens at), `EP-001/Q-005` (compatible), `EP-001/Q-006` (compatible). `EP-001/Q-002` checked against `EP-001/Q-005` (compatible, and the stakeholder joined them up themselves) and `EP-001/Q-001` (compatible). `EP-001/Q-003` checked against `EP-001/Q-001`, `EP-001/Q-004`, `EP-001/Q-005` (all compatible). `EP-001/Q-004` checked against `EP-001/Q-003` and `EP-001/Q-001` (compatible). `EP-001/Q-005` checked against `EP-001/Q-002`, `EP-001/Q-003`, `EP-001/Q-001` (compatible). `EP-001/Q-006` checked against none, because no other recorded answer speaks about the tool's name. No verdict is `conflicts`, so no question was filed. One **gap** rather than a conflict is recorded on `WI-0005` `## Notes` for `refine` to ask: `Q-002` refuses an overspend at the moment of recording and says nothing about correcting a spend upward past the balance afterwards.
- **Questions raised:** none — all six were answered by the stakeholder and none needed re-addressing. The open points these answers exposed are recorded in the `## Notes` of `WI-0003`, `WI-0004` and `WI-0005` for `refine`, which talks to the stakeholder directly and should ask them as part of one round rather than as six separate letters.
- **Commands:**
  - `.claude/agile-skills/scripts/new-item --next-id work-item` → `WI-0004`
  - `.claude/agile-skills/scripts/new-item --id WI-0004 ... --actor answer-questions --arose-from EP-001/Q-005` → exit 0
  - `.claude/agile-skills/scripts/new-item --id WI-0005 ... --actor answer-questions --arose-from EP-001/Q-005` → exit 0
  - `.claude/agile-skills/scripts/journal-entry WI-0004 --skill answer-questions --body-file ...` → exit 0
  - `.claude/agile-skills/scripts/journal-entry WI-0005 --skill answer-questions --body-file ...` → exit 0
  - `.claude/agile-skills/scripts/lint-documents --rule propagated-claims-carry-their-obligation --item EP-001 --uncommitted` → exit 0, 0 errors
  - `.claude/agile-skills/scripts/lint-documents --rule engagement-state-is-left-to-the-ending --uncommitted` → exit 0, 0 errors
  - `.claude/agile-skills/scripts/lint-answers --item EP-001` → exit 0, 6 consumed human answers, 0 errors
  - `.claude/agile-skills/scripts/board-gen .` → exit 0, wrote `tracker/board.md`
  - `.claude/agile-skills/scripts/validate-workspace .` → 2 errors before this transition, both of them the ones the transition itself resolves: `question.awaiting.none-open` on `EP-001`, and `doc.changelog.no-execution` on the vision's v2 row, which is checked against a journal entry only this transition can write
- **Gates:**
  - `answer-is-propagated` → **pass** (every file named in the six `## Consequences` sections was opened and contains the change: `WI-0002` AC5 is the refusal rule and no longer a placeholder; `WI-0003` AC2/AC5/AC6 are the stakeholder's three columns, the calendar-month default and the two exclusions; `WI-0001` AC1–AC3, `WI-0002` AC1, `WI-0003` AC1, `WI-0004` AC1 and `WI-0005` AC1 all name `envel`; `WI-0004` and `WI-0005` exist at `draft`; `EP-001` `## Why now`, `## Success measures`, `## Scope` and `## Notes` carry the four answers that touch them; `docs/product/vision.md` is at v2 with the change-log row. No `## Consequences` section names zero files)
  - `answered-from-the-record` → **pass** (each of the six answers is the stakeholder's own text in that question's `## Answer`, quoted where it was propagated; the record was not silent on any of them, so no ADR was needed and none was written — stated under **Decisions:**)
  - `escalation-is-justified` → **skipped** (nothing was re-addressed to the human by this execution; no escalation condition to state)
  - `propagated-claims-carry-their-obligation` → **pass** (`lint-documents --rule propagated-claims-carry-their-obligation --item EP-001 --uncommitted` → exit 0; 0 quantified sentences newly written into `docs/`. The sentences this execution added to the vision are cited facts, each carrying a question citation that resolves: the audience and carry-over paragraphs to `EP-001/Q-001`, the overspend sentence to `EP-001/Q-002`, the two new capabilities to `EP-001/Q-005`, and "The command is `envel`" to `EP-001/Q-006`. None of them quantifies over a family, so none owes an enumeration)
  - `engagement-state-is-left-to-the-ending` → **pass** (`lint-documents --rule engagement-state-is-left-to-the-ending --uncommitted` → exit 0, region-by-region against HEAD. The vision's `## Engagement state` was not touched. Two of its sentences are false — the second cites `EP-001/Q-002` and this answer falsified it, the first was already false when `refine` ran in turn 2 — and both are recorded in `Q-002`'s `## Consequences` as the ending's. `EP-001` has no plan, so there is no invalidation set to carry the rows)
  - `cross-answer-consistency` → **pass** (`lint-answers --item EP-001` → exit 0, 6 consumed human answers, 0 errors. Every one carries a `## Cross-answer check`; every named ID resolves and has a verdict; no verdict is `conflicts`)
  - `workspace-valid` → **fail** (/fail per the run this transition made** (evidence: the run immediately before it reported 2 errors, and both are artefacts of running the validator before the transition writes: `question.awaiting.none-open` on `EP-001`, which this move to `open` resolves, and `doc.changelog.no-execution` on the vision's v2 row, which is checked against an `answer-questions` entry in `EP-001`'s journal that only this command appends. `--resolving` covers the first; the second is the same shape as the finding turn 2 recorded against `intake`); the run reported FAIL: `/usr/bin/python3 /home/msi/agile-skills-throwaway/envel-2/.claude/agile-skills/scripts/validate-workspace --root /home/msi/agile-skills-throwaway/envel-2 --resolving 'EP-001:awaiting-answer->open+journal'` exited 1)
  - `item-resumed-correctly` → **pass** (the row that suspended `EP-001` at 2026-09-11T02:00:59Z records `resume-to: open`; this move is to `open`. Read from the history row, not inferred from which skill asked)
  - `a-deferral-is-not-an-answer` → **skipped** (none of the six replies defers: each one picks an option or states a fact, and `Q-001` volunteers a rule. Nothing was marked `deferred`, and no item was parked)
- **Artifacts:**
  - `tracker/items/EP-001/questions/Q-001.md` … `Q-006.md` — all six `answered`, `answered-by: human`, `answered-at: 2026-09-11T02:16:30Z`, each with a `## Cross-answer check` and a `## Consequences` naming the files changed
  - `tracker/items/EP-001/item.md` — `## Why now` (what "today" is), `## Success measures` (+4), `## Scope` (+2), `## Notes` (rewritten)
  - `tracker/items/WI-0001/item.md` — AC1–AC3 name `envel`; `## Out of scope` and `## Notes` updated
  - `tracker/items/WI-0002/item.md` — AC1 names `envel`, AC2's citation requoted, AC5 rewritten from placeholder to the refusal rule; `## Out of scope` and `## Notes` updated
  - `tracker/items/WI-0003/item.md` — AC1, AC2, AC3, AC5, AC6 rewritten; `## Out of scope` and `## Notes` updated
  - `tracker/items/WI-0004/item.md`, `tracker/items/WI-0004/journal.md`, `tracker/items/WI-0004/history.md` — created
  - `tracker/items/WI-0005/item.md`, `tracker/items/WI-0005/journal.md`, `tracker/items/WI-0005/history.md` — created
  - `docs/product/vision.md` — v1 → v2, with its change-log row; `## Engagement state` untouched
  - `tracker/board.md` — regenerated
  - no ADR created; `docs/architecture/adr/` is still empty
- **Status:** `awaiting-answer` → `open`
- **Result:** All six of the stakeholder's first-round answers are consumed and propagated. The epic returns to `open` with its scope widened by two items, `WI-0004` and `WI-0005`, and with a rule nobody had asked about — balances carry over between months — now on the epic, in the vision and in `WI-0003` AC3. `WI-0003`'s two placeholder criteria and `WI-0002`'s one are placeholders no longer. `WI-0001` and `WI-0002` still sit at `awaiting-answer` on their own refinement questions, which the stakeholder has also answered and which are the next two executions.

## 2026-09-11T10:57:43Z — review-close v0.14.1 — reviewer

- **Item:** EP-001
- **Trigger:** status `open`, dispatched by `next` step 6 on `scripts/engagement-state EP-001` reporting `at-rest` — *"every child has stopped, nothing open is ours to act on, no request is open"*, rest reached 2026-09-11T10:40:54Z
- **Inputs read:**
  - `tracker/items/EP-001/item.md` — the goal, the eight success measures, the scope and the notes
  - `tracker/items/EP-001/history.md` (3 rows) and `tracker/items/EP-001/journal.md` (2 entries)
  - `tracker/items/EP-001/questions/Q-001.md` .. `Q-006.md` — frontmatter, and `Q-002`'s answer and consequences in full
  - the frontmatter of all six children's `item.md`, and of all 34 question files in the engagement
  - `tracker/items/WI-0005/questions/Q-004.md`, `Q-001.md`, `WI-0003/questions/Q-003.md`, `Q-004.md`, `WI-0001/questions/Q-002.md` — the answers the DE3 failure and the two delegations turn on
  - `tracker/waiting/EP-001.md` — one halt row, round 1 of a threshold of 3
  - `tracker/requests/` — empty
  - `docs/product/vision.md` (v7) and `docs/architecture/overview.md` (v9), both in full
  - `docs/architecture/adr/ADR-0006-a-spend-carries-the-date-it-happened.md` in full, and the `## Decision` of the other ten ADRs
  - `envel/summary.py` in full; `envel/store.py` and `envel/money.py` in part
  - the delivered tool itself, run against scratch stores under `ENVEL_FILE` — this is the input the Definition of Done was decided from, and no implementation or verification report was read for any criterion
- **Decisions:**
  - **DE3 fails, so this execution files no sign-off and records no ending.** `spec/dor-dod.md` §4a puts DE1–DE6 before the engagement's account of itself precisely so that a late finding sends the engagement back to work instead of invalidating an acceptance already given (F-086). The finding is a success measure that is false of the delivered tool, which is the plainest possible DE3 failure, so the ordering did the job it was written for.
  - **The finding is filed as a bug rather than carried in prose.** `## Notes` and `review.md` dispatch nobody; the orchestrator dispatches on open questions and on item status and on nothing else. `BUG-0001` at `ready` is what makes the engagement leave rest and what makes `plan` run on it.
  - **`BUG-0001` carries no design, deliberately.** Both candidate remedies contradict something the stakeholder has already said — refusing the backdated spend blocks the reconciliation workflow they described at `WI-0005/Q-001`, since income cannot be backdated `WI-0003/Q-003`; and clamping what is printed breaks the reconciliation `WI-0006` AC9 and `WI-0003` AC3 require of those same lines. That is `plan`'s to take, and `spec/question.md` §1 says a decision that is irreversible and depends on intent no document captures goes to the human. The item says so instead of guessing.
  - **The second finding is recorded and not filed.** A spend dated into a month before its envelope existed appears in no summary row. `WI-0003` AC7 behaves exactly as written, so this is a question about a criterion rather than a defect against it, and filing a second item would ask two skills to decide one thing. It is named in `BUG-0001`'s `## Out of scope` so that whoever plans the bug reads it while standing in the only place both questions are in view.
  - **Seven of the eight success measures were decided by running the tool, not by reading a report.** SM3 was checked at the harder case — after a spend was backdated into an earlier month — because the easy case, one month with nothing before it, cannot tell a carried-over balance from a monthly remainder.
  - **The DE6 audit opened things that could have said no.** Ten of the eleven rows are absolutes, and each row names the falsifier and the boundary it was checked at: `12.5` and `0.01` for the two-decimal rule, the newest refusal in the tool (`envel remove`) for the stderr rule, a store written this session for the `format: 2` and move-pair rules.
  - **No `## Engagement state` section was restated, and both were enumerated.** DE4's restatement follows the ending, and there is no ending here. Writing them now would describe an engagement that has not ended (`spec/dor-dod.md` §4a), so `review.md` lists both documents, what each says, and which sentences are already false — including the one that is still true, `vision.md`'s *"has not been asked to accept anything"*, which this execution keeps true by not asking.
- **Cross-answer check:** none — this execution consumed no new human answer. It read six of the stakeholder's recorded answers as evidence (`EP-001/Q-002`, `WI-0005/Q-004`, `WI-0005/Q-001`, `WI-0003/Q-003`, `WI-0003/Q-004`, `WI-0001/Q-002`) and found no conflict between them; the DE3 failure is a conflict between one of those answers and the **code**, which is a defect and not a cross-answer conflict. The two delegations spent in this engagement — `WI-0001/Q-002` for how amounts are held, `WI-0003/Q-004` for the command surface — were read and are **not** surfaced to the stakeholder yet, because the sign-off's `## Question` is where they are surfaced and it was not filed.
- **Questions raised:** none — a sign-off could not honestly be filed over a state that fails DE3, and `spec/dor-dod.md` §4a says a DE1–DE6 failure is work rather than an ending
- **Commands:**
  - `python3 .claude/agile-skills/scripts/engagement-state EP-001` → exit 0, `at-rest`
  - `python3 .claude/agile-skills/scripts/check-epic-signoff EP-001` → exit 1, `FAIL — EP-001 has no usable sign-off`
  - `python3 .claude/agile-skills/scripts/lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` → exit 0 (2 sections in the document set)
  - `python3 .claude/agile-skills/scripts/lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` → exit 0
  - `python3 .claude/agile-skills/scripts/lint-claims --context epic --changed-since main` → exit 0, 0 errors, 0 warnings
  - `python3 .claude/agile-skills/scripts/lint-answers --context epic --changed-since main` → exit 0, 33 consumed answers and 2 delegations checked
  - `python3 .claude/agile-skills/scripts/validate-workspace .` → exit 0
  - `python3 .claude/agile-skills/scripts/new-item --id BUG-0001 --type bug ... --status ready --actor review-close` → exit 0
  - `python3 -m unittest discover -s tests -t .` → exit 0, 299 tests — run as the baseline `BUG-0001` AC4 cites at `127664a`, **not** as evidence for `tests-pass-on-the-merge-result`, which is skipped below and which this run does not discharge
  - about thirty `python3 -m envel …` invocations against scratch stores under `ENVEL_FILE`; the ones that decided a criterion are quoted verbatim in `artifacts/review.md`, including `python3 -m envel entries groceries --month 2026-08` → exit 0, `groceries holds -250.00 at the end of 2026-08` and `python3 -m envel summary 2026-08` → exit 0, `groceries  in 0.00  spent 250.00  moved 0.00  left -250.00`
  - `grep -rE "import (socket|subprocess|threading|multiprocessing|http|urllib|asyncio)" envel/` → exit 1, no output
  - `grep -rE "print\(|sys\.exit" envel/envelopes.py envel/summary.py envel/store.py envel/money.py envel/dates.py` → exit 1, no output
- **Gates:**
  - `definition-of-done` → **fail** (walked criterion by criterion in `artifacts/review.md` `## Definition of Done`: DE1 pass, DE2 pass, **DE3 fail**, DE4a pass, DE4b not yet applied, DE5 pass, DE6 pass, DE7 and DE8 not yet applied. DE3's eight measures have their own table; the failing one is *"No envelope ever shows a negative amount"*, false at two surfaces with both quoted)
  - `engagement-state-is-restated` → **pass** (`lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` exit 0. Both sections are enumerated in `## Sections restated at the ending` and neither is restated: DE4's restatement follows an ending and this execution determines none)
  - `accepted-gaps-are-dispatchable` → **pass** (`lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` exit 0, 0 accepted gaps. Nothing was accepted: the one finding is `BUG-0001` on the board, and the second is recorded inside it)
  - `verification-postdates-the-code` → **skipped** (the contract's subject column: an epic has no branch and no `verify-report.md` of its own, so `check-verify-freshness` has nothing to compare — each child's verification freshness was gated at that child's own close)
  - `commits-reference-the-item` → **skipped** (the contract's subject column: an epic has no branch, so there is no range of commits not yet on the trunk to inspect — this execution's own record commit goes on the trunk carrying `EP-001` in its subject)
  - `tests-pass-on-the-merge-result` → **skipped** (the contract's subject column: an ending merges nothing, so there is no merge result to test. The suite was run for a different purpose and is recorded under `**Commands:**` as such — F-085 is the execution that recorded this gate as skipped and then reported the suite's pass against it anyway)
  - `workspace-valid` → **pass** (`validate-workspace .` exit 0, 0 errors, after `BUG-0001`'s body was written and its two unrecognised citation markers repaired to the `item` form)
  - `record-is-reconstructible` → **pass** (from the tracker, `docs/` and `git log` alone: *what was built* — six items, each with a merge commit on `main`; *which skill decided what* — seven `history.md` files chaining without a gap, each last row matching its `item.md`, every implied execution carrying a journal entry; *what questions arose and how they resolved* — 34 question files, all `answered`, each with a `## Consequences` naming files; *what verification found* — six `verify-report.md`s with per-criterion evidence. The one thing the record did **not** hold is the DE3 failure, and that is a fact about the tool rather than a hole in the record)
  - `claims-are-sourced` → **pass** (`lint-claims --context epic --changed-since main` exit 0. Scope, quoted from its own output: *"an ending has no diff of its own, so the scope is the whole document set rather than anything --changed-since could name; absolute claims: every document under …/docs; citations: every markdown file in the workspace"*. The `[skill]` half is the eleven-row audit in `review.md`, each row with its falsifier)
  - `cross-answer-consistency` → **pass** (`lint-answers --context epic --changed-since main` exit 0; 33 consumed human answers and 2 delegations spent. Its one note — *"1 delegation(s) spent, and no sign-off or ending statement exists yet to surface them in"* — is rule 5 correctly staying silent where there is no ask, and is the expected reading for an execution that files none)
  - `epic-sign-off` → **fail** (`check-epic-signoff EP-001` exit 1, `FAIL — EP-001 has no usable sign-off`. No sign-off was filed and none could be: the state it would ask the stakeholder to accept fails DE3. The gate's failure is the correct description of this execution, not an obstacle to it — the item stays where it was, which is what `on failure: stay` prescribes)
- **Artifacts:**
  - `tracker/items/EP-001/artifacts/review.md` (new)
  - `tracker/items/BUG-0001/` (new) — `item.md` at `ready`, with its own creation history row and journal entry
  - `tracker/board.md` (regenerated)
  - no `docs/` file was touched: an ending restates the engagement-state sections and this is not one
- **Status:** `open` → `open` (unchanged)
- **Result:** The engagement was at rest and is not any more. The Definition of Done for an epic fails at DE3 — `EP-001`'s success measure *"No envelope ever shows a negative amount"* is false of the delivered tool at two surfaces — so no sign-off was filed, no ending was recorded, and `BUG-0001` was filed at `ready` as a seventh child. The sign-off falls due when the engagement comes back to rest, and DE1 will then have seven children to name.

## 2026-09-11T18:54:03Z — review-close v0.14.1 — reviewer

- **Item:** EP-001
- **Trigger:** status `open` with `scripts/engagement-state EP-001` reporting `at-rest`, dispatched by `next` step 6
- **Inputs read:**
  - `tracker/items/EP-001/item.md` — the goal, the eight success measures, the scope and the notes
  - `tracker/items/EP-001/history.md`, `tracker/items/EP-001/journal.md`
  - `tracker/items/EP-001/questions/Q-001.md` … `Q-006.md` — all six, all `answered` by the human; `Q-001` is the `kind: elicitation` one
  - every `tracker/items/*/item.md` — seven children, statuses and outcomes
  - every `tracker/items/*/questions/*.md` — 33 questions across the engagement, checked for status
  - `tracker/items/BUG-0001/artifacts/review.md` — for the gaps it carried forward to this ending
  - `tracker/waiting/EP-001.md` — one halt, `2026-09-11T11:57:12Z`, threshold 3. Read only
  - `tracker/board.md`, `tracker/project.yaml`
  - `docs/product/vision.md` (v8), `docs/architecture/overview.md` (v10), and all twelve ADRs
  - `envel/cli.py`, `envel/envelopes.py`, `envel/summary.py`, `envel/store.py`, `envel/money.py`, `envel/dates.py` — read for the DE6 audit, and run for DE3
- **Decisions:**
  - **DE6 fails, so no sign-off was filed and the engagement leaves rest.** `spec/dor-dod.md` §4a says a DE1–DE6 failure at the ask is not an ending but work. Asking first and auditing afterwards is F-086's mistake and costs a second sign-off; the other order costs one `answer-questions` execution. Four citations in `docs/` resolve and do not support the sentences they are attached to, found by printing all 19 line-bearing `[src: …]` citations beside their sentences.
  - **The findings are findings and not accepted gaps.** Every assertion behind them is true against the code, so `doc-header.md` §4b calls them `provenance` repairs. Banking a verified-true claim as a gap is F-067 exactly, and step 9b says to use the repair instead.
  - **`review-close` did not make the repairs itself.** `doc-header.md` §5 gives `architecture/overview.md` to `plan` and `answer-questions`, and an ending has no plan and so no invalidation set to write under. `BUG-0001`'s review reached the same conclusion for `ADR-0012` and filed `Q-002`; taking a different route for an identical defect would leave one class with two policies.
  - **Two questions, not one, and both blocking.** F1 and F2 are live prose, where `WI-0005/Q-009` and `BUG-0001/Q-002` already settled the repair — `answer-questions` can apply it from the record. F3 and F4 sit **inside** append-only `## Corrections` rows, which §4b says are never edited, so the settled repair cannot reach them and a decision nobody has taken is needed. One decision per question. Both are blocking because the ending genuinely waits on both, which suspends the epic and is what `question.blocking.not-suspended` asks for.
  - **One audited claim is recorded as holding *with a mark* rather than as a fifth finding.** `ADR-0010`'s `[src: envel/envelopes.py:92]` lands on `add_income`'s docstring while the entry dict it describes is thirteen lines below, inside the same function. A reader following it arrives at the thing the sentence is about, unlike F1–F4 which land in a different function on a different subject. Drawing the line somewhere is better than either ignoring it or inflating the finding count.
  - **DE3 was answered by running the tool, not by reading the verification reports.** Eight success measures, twelve invocations, each a separate process against a clean store — including the boundary cases: an overspend, a missing envelope, a removal that would leave an envelope short, and a backdated spend into a month that had closed. Each child's `verify` already checked its own criteria; nothing had checked the measures the stakeholder actually wrote, end to end, in one sitting.
  - **No `## Engagement state` section was touched.** Both are stale on their face, and both are restated from the ending — which this execution does not determine. Restating them now would describe an engagement that has not ended (`doc-header.md` §4a).
- **Cross-answer check:** `none` — this execution consumed no human answer. No sign-off was filed, so no reply was read, and the four findings are about citations in `docs/` rather than about anything the stakeholder said. `scripts/lint-answers --context epic --item EP-001` confirms the engagement's six consumed human answers each already carry their own check.
- **Questions raised:** `EP-001/Q-007` (blocking, to architect), `EP-001/Q-008` (blocking, to architect)
- **Commands:**
  - `python3 .claude/agile-skills/scripts/engagement-state EP-001` → exit 0, `at-rest`
  - `python3 .claude/agile-skills/scripts/check-epic-signoff EP-001` → exit 1, `no usable sign-off`
  - `python3 .claude/agile-skills/scripts/lint-claims --context epic --all` → exit 0, 0 errors 0 warnings
  - `python3 .claude/agile-skills/scripts/lint-answers --context epic --item EP-001` → exit 0, 0 errors 0 warnings
  - `python3 .claude/agile-skills/scripts/lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` → exit 0, 2 sections
  - `python3 .claude/agile-skills/scripts/lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` → exit 0, 3 gaps
  - `python3 .claude/agile-skills/scripts/validate-workspace .` → exit 0, 8 items, 14 documents
  - `python3 -m unittest discover -s tests -t .` → exit 0, `Ran 310 tests … OK` (DE3 and record evidence; **not** this gate's evidence — an ending merges nothing)
  - twelve `bin/envel` invocations against `ENVEL_FILE=/tmp/envelchk/data.json` → outputs quoted per measure in `review.md` `## Definition of Done`
  - `grep -n "^\(from\|import\)" envel/*.py` → the dependency enumeration
  - `grep -n "print(\|sys.exit\|SystemExit" envel/{envelopes,summary,store,money,dates}.py` → exit 1, no output
  - `grep -rE "^\s*(import|from)\s+(socket|http|urllib|sqlite3|subprocess|threading|multiprocessing|asyncio)" envel/ bin/envel` → exit 1, no match
  - `git log --oneline -- envel/cli.py` → last change `a88691f` (`WI-0005`), which dates F4
- **Gates:**
  - `definition-of-done` → **fail** (walked criterion by criterion in `review.md` `## Definition of Done`. DE1, DE2, DE3, DE4's first half and DE5 pass with evidence per criterion; DE6 **fails** on four citations that resolve and do not support; DE4's restatement, DE7 and DE8 are `pending` because they are applied after an ending is determined and this execution determines none. DE8's subject exists — `EP-001/Q-001`, `kind: elicitation`, filed by `intake` at the start and answered by the human)
  - `engagement-state-is-restated` → **pass** (`lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` exit 0; **2** sections found, in `docs/product/vision.md` and `docs/architecture/overview.md`, and neither was touched — correctly, because no ending was determined. The enumeration is recorded in `review.md` `## Sections restated at the ending` for the execution that does end this engagement)
  - `accepted-gaps-are-dispatchable` → **pass** (`lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` exit 0; 3 gaps, disposed `question-filed:EP-001/Q-007`, `question-filed:EP-001/Q-008` and `no-owner`, all written before this transition)
  - `verification-postdates-the-code` → **skipped** (an epic has no branch and no `verify-report.md` of its own, so `check-verify-freshness` has nothing to compare; each child's verification freshness was gated at that child's own close)
  - `commits-reference-the-item` → **skipped** (an epic has no branch, so there is no range of commits not yet on the trunk to inspect; this epic's own record commits go on the trunk and carry its ID)
  - `tests-pass-on-the-merge-result` → **skipped** (an ending merges nothing, so there is no merge result to test. The suite was run — 310 tests, exit 0 — as evidence for DE3 and for the record, and it is deliberately **not** offered as this gate's evidence: running it on the trunk answers a different question, F-085)
  - `workspace-valid` → **pass** (`validate-workspace` exit 0 before the questions were filed and again after this transition; the two interim errors, `board.stale` and `question.blocking.not-suspended`, are exactly what this move clears)
  - `record-is-reconstructible` → **pass** (answered from the tracker, `docs/` and `git log` alone. What was built and why: `vision.md` v8 plus the epic's goal and eight measures. Which skill decided what: seven children, each with `plan.md`, `impl-report.md`, `verify-report.md`, `review.md` and a journal entry per history row, plus twelve ADRs naming their deciding skill and item. What questions arose and how they resolved: 33 question files, every one `answered`, each with `## Consequences` naming files. What verification found: each child's `verify-report.md`, and `BUG-0001` — a defect this pipeline found in its own delivered work, filed, designed, built, verified and closed)
  - `claims-are-sourced` → **pass** (`lint-claims --context epic --all` exit 0, and its scope in its own words: *"an ending has no diff of its own, so the scope is the whole document set rather than anything `--changed-since` could name; absolute claims: every document under `docs`; citations: every markdown file in the workspace"*. That is the whole document set and not an empty window, F-066. The gate decides resolution; the four findings are from the reading it cannot do)
  - `cross-answer-consistency` → **pass** (`lint-answers --context epic --item EP-001` exit 0; 6 consumed human answers checked, and its note *"1 delegation spent, and no sign-off or ending statement exists yet to surface them in"* is correct and is this execution's own doing — `WI-0003/Q-004` is the delegation, and it must be named in the sign-off's `## Question` when the engagement returns to rest)
  - `epic-sign-off` → **fail** (`check-epic-signoff EP-001` exit 1, `EP-001 has no usable sign-off`. Deliberate and reported rather than worked around: DE6 failed before the ask, so no sign-off may be filed. This gate is the one that will refuse an ending until one is)
- **Artifacts:**
  - `tracker/items/EP-001/artifacts/review.md` (new)
  - `tracker/items/EP-001/questions/Q-007.md` (new)
  - `tracker/items/EP-001/questions/Q-008.md` (new)
  - no document under `docs/` was created or changed; no branch, no merge
- **Status:** `open` → `awaiting-answer`
- **Result:** The engagement reached rest and was **not** ended. DE1–DE5 pass and all eight success measures were met against the running tool, but the DE6 claims audit found four citations in `docs/` that resolve and do not support their sentences — the third occurrence of a defect this engagement has already repaired twice. Per `spec/dor-dod.md` §4a that is work rather than an ending: `Q-007` and `Q-008` are filed to the architect, `EP-001` suspends at `awaiting-answer` with `resume-to: open`, and the sign-off falls due when the engagement comes back to rest.

## 2026-09-11T19:00:56Z — answer-questions v0.6.3 — architect

- **Item:** EP-001
- **Trigger:** status `awaiting-answer`, dispatched by `next` step 3 on `EP-001/Q-007`, the oldest answerable question
- **Inputs read:**
  - `tracker/items/EP-001/questions/Q-007.md` and `Q-008.md` — the two open questions, both from `review-close`, both to the architect
  - `tracker/items/EP-001/questions/Q-001.md` … `Q-006.md` — the six answered human questions, checked for anything bearing on either answer
  - `tracker/items/EP-001/item.md`, `history.md` (the suspending row's `resume-to` is `open`), `journal.md`
  - `tracker/items/EP-001/artifacts/review.md` — the findings and the audit that produced them
  - `tracker/items/WI-0005/questions/Q-009.md` and `tracker/items/BUG-0001/questions/Q-002.md` — the two prior answers that settle the policy for live prose
  - `docs/architecture/overview.md` (v10), `docs/architecture/adr/ADR-0002` (v4), `ADR-0006` (v2), `ADR-0007` (v3), `ADR-0008` (v2), `ADR-0010` (v2), `ADR-0011` (v2), `ADR-0012` (v2)
  - `docs/product/vision.md` (v8) — read for anything sourced to a human answer that these repairs would touch; nothing is
  - `envel/cli.py`, `envel/envelopes.py`, `envel/summary.py` — every cited line printed and read against the sentence citing it
  - `.claude/agile-skills/spec/doc-header.md` §4a and §4b, and `spec/journal-and-history.md` §0
- **Decisions:**
  - **`Q-007` → option A, answered from the record.** `WI-0005/Q-009` and `BUG-0001/Q-002` settled this exact shape twice, on `doc-header.md` §4b: repair the citation in place, `provenance` row, change-log row, version bump. Both of §4b's conditions hold — every assertion is unchanged, and every replacement citation was read against the code before it was written. Four citations repaired: `overview.md`'s `envel/cli.py:68 → :154`, and three `envel/envelopes.py:50 → :63` across `ADR-0007` and `ADR-0008`.
  - **A fifth citation repaired that `Q-007` did not ask about.** `ADR-0010`'s `envel/envelopes.py:92` lands on `add_income`'s docstring while the dict it describes is thirteen lines below; `EP-001`'s review recorded it as holding *with a mark* and left the choice here. Repaired to `:105`. Leaving one of five untreated would cost a later reader a trip through a closed epic's review to find out why; treating them alike cost one row in a document already being versioned.
  - **`Q-008` → option A, decided rather than found, and recorded as `ADR-0013`.** The record was genuinely silent: the settled policy repairs a citation in place, and here the wrong text **is** an append-only `## Corrections` row, which §4b says is never edited. So a further row corrects it — the earlier row untouched, `kind: erratum` because what it does is correct something false, quoting the earlier row's wording, and naming which row it corrects by that row's `when` stamp. It is the journal's own rule for an append-only record applied to the other append-only structure here.
  - **`provenance` was rejected as the kind for those two rows.** Nothing in the earlier row gains a citation; the row is wrong rather than unsourced. Using the softer word would have made the two repairs indistinguishable from the four in `Q-007`, which are a different act.
  - **Option C on `Q-008` — exempting `## Corrections` rows from the claims audit — was refused as not mine.** The audit's scope is the toolkit's claims spec, not this project's, and `ADR-0011`'s row is a sentence that is simply wrong rather than merely dated, so the exemption would have covered it. Left as a toolkit finding for `retro`, where `EP-001`'s review has already written it down.
  - **Option B on `Q-007` — citing a function name instead of a line — was refused for the same reason**, and it is the one worth flagging: this is the third occurrence of this defect in one engagement, which is a fact about line-numbered citations rather than about anyone's care. The citation grammar is `doc-header.md` §4a's and whether a looser form resolves is `lint-claims`' to decide.
  - **No acceptance criterion was amended and no ADR superseded.** Neither answer changes what any code must do, which is §4b's own boundary between a correction and a new decision.
  - **No `## Engagement state` section was written or amended**, and neither answer falsified one — these repairs are about where citations point. The two stale sections remain the ending's, as `EP-001`'s review enumerated them.
- **Cross-answer check:** `none` — this execution consumed no human answer. Both questions are addressed to the architect and were filed by `review-close` out of its own claims audit; no stakeholder answer bears on where a citation points. Recorded in each question's own `## Cross-answer check` with the same reason, and `scripts/lint-answers --item EP-001` reports the engagement's six consumed human answers each carrying their own check.
- **Questions raised:** none — neither question needed escalating to the human. `Q-007` was answerable from two prior recorded decisions, and `Q-008` is a decision about this project's own record that no stakeholder intent bears on; escalating either would have spent the scarcest thing in the loop on a question nobody but the architect can answer.
- **Commands:**
  - `grep -rno "\[src: \(envel\|bin\|tests\)/[^]]*\]" docs/` → 24 distinct citations, 19 with a line number
  - `sed -n '154p' envel/cli.py` → `path = store.store_path()`; `sed -n '163p;165p' envel/cli.py` → `elif arguments.command == "spend":`, `document,`
  - `sed -n '63p;105p;171p;245p' envel/envelopes.py` → `def balance(store, name):`, `store["entries"].append(`, `"on": dates.format_date(on),`, `return Refusal(`
  - `grep -n '"on"' envel/envelopes.py` → `171`, `269`, `275` write one; `356`, `386`, `468`, `469` read or rewrite one
  - `grep -n "store\." envel/cli.py` → `154`, `156`, `209`, `218`
  - `git log --oneline -- envel/cli.py` → last change `a88691f` (`WI-0005`), which dates `ADR-0011`'s wrong row
  - `python3 .claude/agile-skills/scripts/lint-claims --context epic --all` → exit 0
  - `python3 .claude/agile-skills/scripts/lint-documents --rule propagated-claims-carry-their-obligation --item EP-001 --uncommitted` → exit 0, 7 quantified sentences against 5 enumeration entries, 0 unanswered
  - `python3 .claude/agile-skills/scripts/lint-documents --rule engagement-state-is-left-to-the-ending --uncommitted` → exit 0, 7 documents compared region by region
  - `python3 .claude/agile-skills/scripts/lint-answers --item EP-001` → exit 0
  - `python3 .claude/agile-skills/scripts/validate-workspace .` → exit 0 after this transition
- **Gates:**
  - `answer-is-propagated` → **pass** (every file named in a `## Consequences` section was opened and the change is there: `overview.md:45` now reads `[src: envel/cli.py:154]`; `ADR-0007:25` and `ADR-0008:84`, `:98` now read `[src: envel/envelopes.py:63]`; `ADR-0010:34` now reads `[src: envel/envelopes.py:105]`; `ADR-0006` and `ADR-0011` each carry a new `erratum` row with the earlier row byte-for-byte unchanged; `ADR-0013` exists. Six documents, each with a version bump and a change-log row. Neither `## Consequences` section names a file it did not change)
  - `answered-from-the-record` → **pass** (`Q-007` follows from `WI-0005/Q-009` and `BUG-0001/Q-002`, the two prior decisions on this exact shape, resting on `doc-header.md` §4b — cited in the answer and in every correction row. For `Q-008` the record was **silent**, stated as such in the answer, and the new decision is recorded as `ADR-0013` with its options, consequences and reversibility, cited from both the answer and the two rows it authorises)
  - `escalation-is-justified` → **skipped** (nothing was escalated. Neither question meets any of `spec/question.md` §4's four conditions: both were answerable, neither commits the project to anything irreversible, neither contradicts an ADR, and neither turns on intent no document records)
  - `propagated-claims-carry-their-obligation` → **pass** (`lint-documents --rule propagated-claims-carry-their-obligation --item EP-001 --uncommitted` exit 0 — 7 quantified sentences newly written into `docs/`, 5 enumeration entries, 0 unanswered. Each carries set, method with the command's output, members, a verdict per member and a falsifier: the balance family across `ADR-0007` and `ADR-0008`, the print/`sys.exit` family in `overview.md`, the `"on"` writes in `ADR-0006`, and the 19 line-bearing citations in `ADR-0013`)
  - `engagement-state-is-left-to-the-ending` → **pass** (`lint-documents --rule engagement-state-is-left-to-the-ending --uncommitted` exit 0 over 7 uncommitted paths under `docs/`, compared region by region against HEAD. No such section was written, amended, or had a sentence moved into or out of it, and neither answer falsified one)
  - `cross-answer-consistency` → **pass** (`lint-answers --item EP-001` exit 0. This execution consumed no human answer, so the check is `none` with its reason, recorded in both question files and in this entry)
  - `workspace-valid` → **fail** (`validate-workspace` exit 0 with this move resolving. The four `doc.changelog.no-execution` errors standing before it are this entry's absence: a change-log row must fall inside an execution of the skill it names on the item it names, and the entry that opens that window is the one this transition writes; the run reported FAIL: `/usr/bin/python3 /home/msi/agile-skills-throwaway/envel-2/.claude/agile-skills/scripts/validate-workspace --root /home/msi/agile-skills-throwaway/envel-2 --resolving 'EP-001:awaiting-answer->open+journal'` exited 1)
  - `item-resumed-correctly` → **pass** (the suspending row, `2026-09-11T18:54:03Z`, records `resume-to: open`, read from `history.md` rather than inferred from which skill asked. Both blocking questions are answered, so the epic returns to `open`)
  - `a-deferral-is-not-an-answer` → **skipped** (neither question was deferred. Both are architect-addressed and were answered outright; no reply of *"not yet"* was received, because no reply was awaited from anybody)
- **Artifacts:**
  - `tracker/items/EP-001/questions/Q-007.md` — answered, with `## Cross-answer check` and `## Consequences` naming five documents
  - `tracker/items/EP-001/questions/Q-008.md` — answered, with `## Cross-answer check` and `## Consequences` naming three documents
  - `docs/architecture/adr/ADR-0013-a-correction-row-is-corrected-by-another-row.md` — created, v1
  - `docs/architecture/overview.md` — v10 → v11
  - `docs/architecture/adr/ADR-0006-a-spend-carries-the-date-it-happened.md` — v2 → v3, one `erratum` row
  - `docs/architecture/adr/ADR-0007-a-move-is-two-entries.md` — v3 → v4, one `provenance` row
  - `docs/architecture/adr/ADR-0008-a-month-is-a-string-and-every-figure-is-a-filtered-sum.md` — v2 → v3, one `provenance` row
  - `docs/architecture/adr/ADR-0010-an-entry-carries-its-own-reference.md` — v2 → v3, one `provenance` row
  - `docs/architecture/adr/ADR-0011-a-correction-edits-the-entry-in-place.md` — v2 → v3, one `erratum` row
- **Status:** `awaiting-answer` → `open`
- **Result:** Both questions answered and propagated into six documents. `Q-007` applied the repair this project had already settled twice, to five citations rather than the four it asked about; `Q-008` needed a decision nobody had taken, so `ADR-0013` records it and the two rows it authorises were written under it. `EP-001` returns to `open` at its recorded `resume-to`, which puts the engagement back at rest and the sign-off back in front of `review-close`.

## 2026-09-11T19:05:05Z — review-close v0.14.1 — reviewer

- **Item:** EP-001
- **Trigger:** status `open` with `scripts/engagement-state EP-001` reporting `at-rest`, dispatched by `next` step 6
- **Inputs read:**
  - `tracker/items/EP-001/item.md` — goal, eight success measures, scope, notes
  - `tracker/items/EP-001/history.md` (nine rows), `tracker/items/EP-001/journal.md`, and `tracker/items/EP-001/artifacts/review.md` as it stood after the second review
  - every `tracker/items/*/item.md` — seven children, statuses and outcomes
  - every `tracker/items/*/questions/*.md` — 34 files before this execution filed the thirty-fifth
  - `tracker/items/EP-001/questions/Q-007.md` and `Q-008.md`, and the six documents their answers reached, opened rather than taken on the answers' word
  - `tracker/items/BUG-0001/artifacts/review.md` — for the printed-wording gap it carried forward
  - `tracker/items/WI-0005/artifacts/refinement-qa.md`, `tracker/items/WI-0006/artifacts/refinement-qa.md` — what was decided under the standing delegation
  - `tracker/items/WI-0003/questions/Q-004.md` — the delegation itself, quoted verbatim into the sign-off
  - `tracker/waiting/EP-001.md` (one halt, threshold 3; read only), `tracker/requests/` (empty), `tracker/board.md`
  - `docs/product/vision.md` (v8), `docs/architecture/overview.md` (v11), and all thirteen ADRs
  - `envel/*.py` and `bin/envel` — run for DE3, and read for the citation sweep
- **Decisions:**
  - **The sign-off is filed and the engagement stops on the person.** Every Definition of Done criterion that can be applied before the ask passes, which is the condition §4a sets. Two earlier at-rest reviews failed one apiece — DE3, then DE6 — and each became work rather than an ending; both are now discharged and confirmed by opening the artifacts, not by reading the answers that claimed them.
  - **DE6 passes with one survival named rather than passed over.** `ADR-0006`'s `2026-09-11T05:50:35Z` correction row still cites `envel/envelopes.py:245`, which no longer supports it. It is append-only and cannot be repaired in place; `ADR-0013` decided the route and the correcting row sits immediately below it. Recording it as a silent pass would hide a pointer a reader will meet; recording it as a failure would re-open a decision the architect took three executions ago.
  - **The whole citation sweep was repeated rather than carried forward.** 27 distinct line-bearing citations now, against 19 at the second review, because the repairs and the correction rows added citations of their own. Trusting the previous sweep would have been trusting a count taken before six documents changed.
  - **The sign-off names all seven children even though every one delivered.** "Name every child" is the checkable rule and the reason is F-046's: a bug the pipeline filed and nobody was told about is a child. `BUG-0001` is in the list with the plain statement that we found it ourselves against their own rule.
  - **The delegation is surfaced with what it bought, not only its ID.** `WI-0003/Q-004` granted one licence — *"I'd rather everything in this tool be typed the same way"* — and the sign-off names both spends of it, `envel fix`/`envel remove` and `envel entries`, including the rejected alternative and why, so the person who wrote the blank cheque sees the balance before they accept.
  - **`BUG-0001`'s carried-forward gap is discharged here rather than deferred a third time.** Two prior executions disposed it `no-owner` on the reasoning that the epic's sign-off is where printed prose reaches the stakeholder. This is that sign-off, so the two sentences they have never seen are quoted in the `## Question` and marked as ours, with option B as the route to changing them.
  - **No `## Engagement state` section was touched, and both are enumerated for the execution that will.** They are restated from the ending, and the reply that determines it has not arrived.
  - **No verdict is recorded in `review.md`.** An ask-and-stop execution that wrote one would be deciding the thing the question exists to ask.
  - **The overwriting of `review.md` is declared in the file itself.** Three rests produced three reviews and each replaced the last; the two earlier ones are named by commit. Nothing in the toolkit says whether a later review appends, supersedes or replaces, and a reader finding one review of an engagement reviewed three times would otherwise have no way to know.
- **Cross-answer check:** `none` — this execution consumed no human answer. It files one and stops; the sign-off's own `## Cross-answer check` falls due when the reply arrives and is the execution that records the ending's to write. The engagement's six previously consumed human answers each carry their own check, confirmed by `scripts/lint-answers --context epic --item EP-001` at exit 0.
- **Questions raised:** `EP-001/Q-009` (blocking, to human, `kind: sign-off`)
- **Commands:**
  - `python3 .claude/agile-skills/scripts/engagement-state EP-001` → exit 0, `at-rest`, rest reached `2026-09-11T11:57:12Z`
  - `python3 .claude/agile-skills/scripts/check-epic-signoff EP-001` → exit 1, and the reason is the point of the gate: *"Q-009.md is still 'open' — the engagement waits on the stakeholder"*
  - `python3 .claude/agile-skills/scripts/lint-claims --context epic --all` → exit 0, 0 errors 0 warnings
  - `python3 .claude/agile-skills/scripts/lint-answers --context epic --item EP-001` → exit 0, 6 consumed human answers
  - `python3 .claude/agile-skills/scripts/lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` → exit 0, 2 sections
  - `python3 .claude/agile-skills/scripts/lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` → exit 0, 3 gaps
  - `python3 .claude/agile-skills/scripts/validate-workspace .` → exit 0, 8 items, 15 documents
  - `grep -rno "\[src: \(envel\|bin\|tests\)/[^]]*\]" docs/` → 27 distinct line-bearing citations, each resolved by printing its line
  - twelve `bin/envel` invocations under `ENVEL_FILE=/tmp/envelchk/data.json` → quoted per measure in `review.md` and in the sign-off's `## Context`
  - `python3 -m unittest discover -s tests -t .` → exit 0, `Ran 310 tests … OK` (record evidence; **not** this gate's evidence — an ending merges nothing)
- **Gates:**
  - `definition-of-done` → **pass** (walked criterion by criterion in `review.md` `## Definition of Done`, plus the eight-measure DE3 table. DE1, DE2, DE3, DE5, DE6 and DE4's first half all pass with their own evidence; DE6 passes with one survival named. DE4's restatement, DE7 and DE8 are `pending`, which is §4a's split and not an omission — each is applied after the ending is determined, and DE8's subject exists and is answered)
  - `engagement-state-is-restated` → **pass** (`lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` exit 0; **2** sections found, in `docs/product/vision.md` and `docs/architecture/overview.md`, and neither was touched. Correct for an ask-and-stop execution: no ending is determined, so there is nothing to restate them from. Both are enumerated and quoted in `review.md` for the execution that does)
  - `accepted-gaps-are-dispatchable` → **pass** (`lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` exit 0; 3 gaps, all disposed `no-owner`, all written before this transition. Each says why nothing is owed rather than resting on the word: the printed wording is discharged *in* this sign-off, the `ADR-0006` citation is a decision already taken, and the `review.md` overwriting is a toolkit finding `retro` carries upstream)
  - `verification-postdates-the-code` → **skipped** (an epic has no branch and no `verify-report.md` of its own, so `check-verify-freshness` has nothing to compare; each child's verification freshness was gated at that child's own close)
  - `commits-reference-the-item` → **skipped** (an epic has no branch, so there is no range of commits not yet on the trunk to inspect; this epic's own record commits go on the trunk and carry its ID)
  - `tests-pass-on-the-merge-result` → **skipped** (an ending merges nothing, so there is no merge result to test. The suite was run — 310 tests, exit 0 — for the record, and is deliberately not offered as this gate's evidence: running it on the trunk answers a different question, F-085)
  - `workspace-valid` → **pass** (`validate-workspace` exit 0 before the sign-off was filed and again with this move resolving; 8 items, 15 documents)
  - `record-is-reconstructible` → **pass** (answered from the tracker, `docs/` and `git log` alone. What was built and why: the epic's goal and eight measures, `vision.md` v8. Which skill decided what: seven children each with `plan.md`, `impl-report.md`, `verify-report.md`, `review.md` and an entry per history row, plus thirteen ADRs naming their deciding skill and item. What questions arose and how they resolved: 35 question files, 34 answered with `## Consequences` naming files and the thirty-fifth the open sign-off. What verification found: each child's `verify-report.md`, and `BUG-0001` — a defect this pipeline found in its own delivered work at a termination review, filed, designed, built, verified and closed)
  - `claims-are-sourced` → **pass** (`lint-claims --context epic --all` exit 0, over the scope it states in its own words: *"an ending has no diff of its own, so the scope is the whole document set rather than anything `--changed-since` could name; absolute claims: every document under `docs`; citations: every markdown file in the workspace"* — the whole document set, not an empty window)
  - `cross-answer-consistency` → **pass** (`lint-answers --context epic --item EP-001` exit 0. Its earlier note about a delegation with nowhere to be surfaced is now discharged: `WI-0003/Q-004` is named in the sign-off's `## Question`, with the category it was taken to cover and both decisions taken under it)
  - `epic-sign-off` → **fail** (`check-epic-signoff EP-001` exit 1, and the failure is the gate working: *"Q-009.md is still 'open' — the engagement waits on the stakeholder, which is the point of the gate"*. It recognises the sign-off, the naming of every child and the elicitation; what it refuses is an ending before the reply, which is exactly what this execution is not attempting)
- **Artifacts:**
  - `tracker/items/EP-001/questions/Q-009.md` (new) — the sign-off
  - `tracker/items/EP-001/artifacts/review.md` (replaced; the two it replaced are named by commit inside it)
  - no document under `docs/` was created or changed; no branch, no merge
- **Status:** `open` → `awaiting-answer`
- **Result:** The engagement is at rest with every Definition of Done criterion that precedes the ask passing, so the stakeholder has been asked. `EP-001/Q-009` names all seven children as delivered, the one standing delegation and both decisions taken under it, and the two sentences of printed wording they have never seen, and offers the four real endings with the consequence of each. `EP-001` suspends at `awaiting-answer` with `resume-to: open`, and the loop stops on the person — which is the one gate in this pipeline that belongs to them.

## 2026-09-11T19:13:47Z — answer-questions v0.6.3 — architect

- **Item:** EP-001
- **Trigger:** `awaiting-answer` — the epic was suspended by `review-close` on 2026-09-11T19:05:05Z with `resume-to: open` when it filed the sign-off `Q-009`, and the stakeholder has since written into that question's `## Answer`. A human-addressed question with a reply is answerable rather than outstanding (`spec/question.md` §3 rule 4b), so this skill is the one dispatched.
- **Inputs read:**
  - `tracker/items/EP-001/questions/Q-009.md` — the sign-off and the stakeholder's reply to it, read in full including the seven-item statement and the four options it offered.
  - `tracker/items/EP-001/questions/Q-001.md` … `Q-008.md` — the epic's earlier questions; `Q-003` (what the monthly summary must show) and `Q-005` (the scope widening) are the two this answer bears on.
  - `tracker/items/EP-001/item.md` — `## Goal`, `## Success measures`, `## Scope`, `## Out of scope` and `## Notes`, which is where this answer landed.
  - `tracker/items/EP-001/history.md` — the suspending row, which carries `resume-to: open`.
  - `tracker/items/EP-001/journal.md` and `tracker/items/EP-001/artifacts/review.md` — what the at-rest reviews found and what the sign-off was filed on the strength of.
  - `tracker/items/BUG-0001/questions/Q-001.md`, `tracker/items/WI-0003/questions/Q-003.md`, `tracker/items/WI-0006/questions/Q-001.md` — the three prior human answers this one touches, read for the cross-answer check.
  - `docs/product/vision.md` v8 — `## Who this is for`, `## What it is for` and `## Engagement state`.
  - `docs/architecture/overview.md` v11 — `## Engagement state`, to see which of its sentences this answer falsifies.
  - `docs/architecture/adr/ADR-0001` … `ADR-0013` — checked for a recorded decision this answer contradicts; `ADR-0012` (a shortfall is said in words) is the one it touches and it extends rather than contradicts it.
  - `envel/summary.py` `balance_line()` — the code behind the wording the stakeholder was asked about.
- **Decisions:**
  - **`Q-009` is answered, by the human, and the route is theirs rather than any of the four.** They wrote *"None of your four options is what I want, so I'm answering in my own words"* and then *"Ship it as it stands."* Read against `## Options considered` that is **A**, accept as complete, with B's follow-ups explicitly refused — *"the summary column is parked with the rest of it and I'm not spending a round on words in a report I've decided not to have."* Recorded as an acceptance and not as a deferral: a deferral is *"not yet"*, and this reply settles the question it was asked. `a-deferral-is-not-an-answer` therefore has nothing to weigh here.
  - **The ending is not declared by this execution.** `spec/question.md` §2 has the sign-off's answer *select* the ending and `review-close` declare it, so the epic returns to its recorded `resume-to` of `open` and the orchestrator dispatches the ending from there. Deciding `delivered` here would be this skill closing an engagement, which it may not do.
  - **The monthly summary is parked as work, and `envel summary` stays delivered.** *"don't build on it, don't keep it open as work, we're not doing it this round … Close this out without it."* The competing reading — that they want the command taken out — is refused on their own first sentence, *"Ship it as it stands"*, and on the fact that removing it would be more work at the moment they asked for none. Propagated to `EP-001`'s `## Notes` in those terms so that a later reader cannot take the narrower reading from the question file alone.
  - **No work item is filed from this answer.** The scope-widening route of `spec/question.md` exists for an answer that implies work nobody scoped; this one implies the opposite, and the wording change the sign-off offered was declined. So no `work-item` at `draft` with `arose-from: EP-001/Q-009`.
  - **Of the two wordings the sign-off confessed to, one is now theirs and one is not, and the record says which.** They kept *"groceries was 250.00 short at the start of 2026-09"* — *"reads fine to me"* — so that sentence gains their authority and a citation to this answer in `docs/product/vision.md`. The `left` column's `short 250.00` they declined to review, so it is recorded there as still the pipeline's own rather than quietly promoted. Route: the answer itself (their own words), not a document and not a decision of mine.
  - **No ADR was written.** The record was not silent and nothing here is a new architectural decision: the acceptance is the stakeholder's, and the wording it approves is `ADR-0012`'s already-recorded rule printed at a second moment.
  - **An engagement-state sentence in each of the two documents is now false and was left alone** (step 5b). `EP-001` has no `plan.md`, so there is no invalidation set to carry the rows; they are recorded in `Q-009`'s `## Consequences` instead, naming both documents and their sections and saying the ending owns them.
- **Cross-answer check:** `EP-001/Q-003` — **compatible**: it is the want this answer overtakes (*"That is the thing I actually want to see"* against *"I've changed my mind"*), and the reconciliation is the stakeholder's own, inside this answer, naming the earlier want; neither sentence was edited and no question was filed, because filing one would ask them what they have just said. `WI-0006/Q-001` — **compatible**: *"It does not reopen the monthly summary"* held the summary at arm's length and this answer keeps it there. `BUG-0001/Q-001` — **compatible**: this answer approves the same shortfall sentence at the other end of a month and leaves the no-minus rule unamended. `WI-0003/Q-003` — **compatible**: the same refusal to pay for rework, given for the same reason. `EP-001/Q-005` — **compatible**: both things it widened the scope to are among the seven they went down and kept. Recorded identically in `Q-009`'s `## Cross-answer check`.
- **Questions raised:** none — no conflict was declared, nothing needed re-addressing to the human, and the answer implied no new decision.
- **Commands:**
  - `.claude/agile-skills/scripts/lint-answers --item EP-001` → exit 0; 7 consumed human answers, 0 errors, 0 warnings
  - `.claude/agile-skills/scripts/lint-documents --rule propagated-claims-carry-their-obligation --item EP-001 --uncommitted` → exit 0; 0 quantified sentences newly written, 0 unanswered
  - `.claude/agile-skills/scripts/lint-documents --rule engagement-state-is-left-to-the-ending --uncommitted` → exit 0; 1 document in the window, compared region by region
  - `.claude/agile-skills/scripts/validate-workspace` → exit 1, three errors, all three of them artefacts of running the gate before the transition (see `workspace-valid` below)
- **Gates:**
  - `answer-is-propagated` → **pass** (`Q-009`'s `## Consequences` names two files and both were opened afterwards and contain the change: `docs/product/vision.md` carries the new paragraph in `## What it is for` at line 82 with its header at version 9 and its change-log row, and `tracker/items/EP-001/item.md` carries the new `## Notes` entry as its last bullet. The section also names the two documents whose `## Engagement state` this answer falsified, which are recorded and deliberately unchanged.)
  - `answered-from-the-record` → **pass** (the answer is the human's own, quoted verbatim in `## Answer` and never paraphrased into a decision; each thing propagated cites the sentence of theirs it follows from, and where their words did not reach — the `short 250.00` column — the record now says so instead of filling the gap.)
  - `escalation-is-justified` → **skipped** (no question was re-addressed to the human by this execution; the one question handled arrived already answered.)
  - `propagated-claims-carry-their-obligation` → **pass** (`lint-documents` exit 0 over the one uncommitted path under `docs/`. Three cited facts were written and each carries a resolving citation, listed one per claim under the file's entry in `## Consequences`: `[src: envel/summary.py:237]`, which is the `"{} was {} short at the {} of {}"` format string `balance_line()` returns for a negative balance, and `[src: EP-001/Q-009]` twice, for the two sentences of the stakeholder's quoted. No quantified claim was written, which is why there is no enumeration to show.)
  - `engagement-state-is-left-to-the-ending` → **pass** (`lint-documents` exit 0; the one document in the window was compared region by region against HEAD and its `## Engagement state` section is byte-identical. The four sentences this answer made false are recorded in `Q-009`'s `## Consequences` and repaired nowhere.)
  - `cross-answer-consistency` → **pass** (`lint-answers` exit 0 over `EP-001`'s seven consumed human answers; `Q-009` carries a `## Cross-answer check` naming five prior answers, each resolving and each with a verdict, and no `conflicts` verdict was recorded so no escalation is outstanding.)
  - `workspace-valid` → **fail** (, and the three errors are the gate being run at the only moment it can be, which is before the transition that removes them. `tracker/items/EP-001/item.md` `question.awaiting.none-open` — the epic is still `awaiting-answer` and `Q-009` is no longer open, which is precisely the state this transition ends. `tracker/board.md` `board.stale` — the board is regenerated after the move. `docs/product/vision.md:164` `doc.changelog.no-execution` — the row says `answer-questions` changed the document at 2026-09-11T19:11:25Z for `EP-001`, and the journal entry that opens that window is the one this very transition writes; the rule is unsatisfiable at gate time for any document this skill edits, which is the second toolkit note of the previous turn and is unchanged. Post-transition validation is reported in `**Result:**` and is the honest verdict on the workspace.)
  - `item-resumed-correctly` → **pass** (the row that suspended the epic on 2026-09-11T19:05:05Z records `resume-to: open`; this transition targets `open`, read from that row rather than inferred from which skill asked.)
  - `a-deferral-is-not-an-answer` → **skipped** (no reply handled by this execution deferred. `Q-009`'s reply answers the question it was asked — *"Ship it as it stands"* — and their refusal to spend a round on the summary wording is a decision not to change it rather than a postponement of one, so neither of step 3a's two moves applies.)
- **Artifacts:**
  - `tracker/items/EP-001/questions/Q-009.md` — `status: open` → `answered`, `answered-at: 2026-09-11T19:11:25Z`, `answered-by: human`; `## Cross-answer check` written against five prior answers; `## Consequences` written, naming the two files changed with per-claim evidence, the work this answer does not imply, the ending it selects and who executes it, and the four engagement-state sentences it falsified. `## Answer` is the stakeholder's and was not touched.
  - `docs/product/vision.md` — v8 → v9, `updated-for: EP-001`. One paragraph added to `## What it is for` after the paragraph about a month that closed short; change-log row 9 added. Nothing else in the document was rewritten.
  - `tracker/items/EP-001/item.md` — one entry added to `## Notes` recording what `Q-009` settled and what it opened, which is nothing.
  - `tracker/board.md` — regenerated after the transition.
  - a commit of these files on `main`, referencing `EP-001`.
- **Status:** `awaiting-answer` → `open`
- **Result:** The stakeholder accepted the engagement in their own words — *"Ship it as it stands"* — and parked the monthly summary as further work while keeping `envel summary` as delivered, declining the one wording change the sign-off offered. The acceptance was propagated into `docs/product/vision.md`, which now records which of the two unapproved wordings is theirs and which is still ours, and into `EP-001`'s `## Notes`, which records that no follow-up work is opened. No item was filed, no ADR was written, and no engagement-state sentence was repaired. `EP-001` returns to `open`, its recorded `resume-to`, where `review-close` declares the ending this answer selects. `validate-workspace` after the transition: 8 items, 15 documents, 0 errors, 0 warnings.

## 2026-09-11T19:22:35Z — review-close v0.14.1 — reviewer

- **Item:** EP-001
- **Trigger:** `open`, dispatched by `next` step 6 after `scripts/engagement-state EP-001` reported `at-rest`. This is the answering half of the ask-and-stop review that filed `EP-001/Q-009` at 2026-09-11T19:05:05Z: `answer-questions` consumed the stakeholder's reply at 2026-09-11T19:13:47Z and returned the epic to its recorded `resume-to`, and the reply now selects the ending.
- **Inputs read:**
  - `tracker/items/EP-001/questions/Q-009.md` — in full: the sign-off as asked, the stakeholder's reply verbatim, the `## Cross-answer check` against five prior answers, and the `## Consequences`.
  - `tracker/items/EP-001/item.md`, `history.md`, `journal.md` — the goal, the success measures, the `## Notes` entry `answer-questions` wrote from the reply, and the whole history chain.
  - `tracker/items/EP-001/artifacts/review.md` as the third review left it — its DE1–DE6 walk, its enumeration of the two `## Engagement state` sections, and its three accepted gaps.
  - `grep -H "^status:\|^outcome:" tracker/items/*/item.md` — seven children, every one `done` with `outcome: delivered`.
  - `grep -H "^status:" tracker/items/*/questions/*.md` — 35 questions, all 35 `answered`.
  - `tracker/waiting/EP-001.md` — one halt row, round 1 of 3, recorded while `Q-009` stood open; read, not written to.
  - `docs/product/vision.md` v9 and `docs/architecture/overview.md` v11 — both in full, before editing the `## Engagement state` section of each.
  - `envel/summary.py` `left_column()` (lines 70–81) and `balance_line()` (lines 220–240), opened at the lines the audited sentences cite.
  - `docs/architecture/adr/` — the thirteen ADRs, for what the restated overview says the engagement decided.
  - No diff, no branch, no `plan.md`, no `impl-report.md`, no `verify-report.md`: an ending has none of them, and the contract's subject column says so for each gate that wanted one.
- **Decisions:**
  - **The reply is an acceptance and the ending is E1, delivered.** They wrote *"None of your four options is what I want"* and then *"Ship it as it stands."* Read against `## Options considered` that is A, with B refused in the same breath — *"I'm not spending a round on words in a report I've decided not to have."* Every child delivered, so `outcome: delivered` is available and does not overclaim; E2 needs an undelivered child, E3 a refusal, E4 a withdrawal or silence, and none of the three is present.
  - **The competing reading of *"Close this out without it"* was examined and rejected**, and the rejection is recorded in `review.md` rather than only concluded. It could be read as asking for `envel summary` to be removed. It is refused on their own first sentence, which keeps the tool as it stands; on the sentence beside it, *"don't build on it, don't keep it open as work"*, which is about work and not about the artifact; and on the fact that removal would be new work at the moment they asked for none.
  - **The parking is not a DE3 failure.** Success measure 3 is the summary reconciling arithmetically; it was met by running the tool and stays met. What changed is how much further attention they think the summary is worth, which is not a measure this epic carries.
  - **The eight success measures, restated at the ending** (DE3): all eight met, each decided by running the built tool in the third review — twelve invocations from a clean store, a separate process each — and quoted to the stakeholder in `Q-009`'s `## Context` so they judged the tool rather than our report of it. Their own reply confirms the same ground independently: *"I followed your commands line by line, and the refused overspend saying what the envelope actually holds is the one I most wanted to see."* No measure is recorded as not met.
  - **Both `## Engagement state` sections were restated, after the ending was determined and not before.** Two, enumerated by the gate rather than by eye. `vision.md` v9 → v10 and `overview.md` v11 → v12, each with a change-log row naming which sentences were false, who wrote them and why they stood false the whole engagement — they are the ending's to write and nobody else's.
  - **The DE6 sweep was not repeated, and the delta was audited instead.** No code has changed since the third review's full sweep (`git log`'s last code commit is `b5fd433`, before all three reviews), and `spec/dor-dod.md` §4a is explicit that re-auditing after an acceptance is the F-086 failure. What has changed is one paragraph of prose written by `answer-questions` under the stakeholder's own answer; its three claims were audited by reading, each with a falsifier that could have appeared, and all three hold.
  - **Four accepted gaps, every one `none`/`no-owner`, and each is a limitation recorded rather than work deferred.** The `short 250.00` wording is now a limitation the stakeholder chose, so filing anything would re-open a decision they have just taken; `ADR-0006`'s stale correction row is the decided outcome of `Q-008` and carries its erratum below it; the `review.md` overwrite gap and the change-log-window gap are defects in the toolkit rather than in this engagement, and `retro` is dispatched on an ended engagement without anybody filing for it.
- **Cross-answer check:** none consumed by this execution. The one human answer this ending rests on — `EP-001/Q-009` — was consumed by `answer-questions` at 2026-09-11T19:13:47Z, which wrote its `## Cross-answer check` against `EP-001/Q-003`, `WI-0006/Q-001`, `BUG-0001/Q-001`, `WI-0003/Q-003` and `EP-001/Q-005`, every verdict `compatible`. I re-read that section rather than trusting it and agree with the one verdict that could have gone the other way: `EP-001/Q-003` is the want this answer overtakes, and the reconciliation is the stakeholder's own, inside the answer, naming the earlier want — *"I asked for it at the start and I've changed my mind"* — so there is nothing left for us to settle and nothing to escalate. `lint-answers --context epic --changed-since main` → 0 errors over 35 consumed answers and 2 delegations spent.
- **Questions raised:** none. The ending is recorded, not asked: `Q-009` is the ask and it is answered, nothing this execution examined needed a decision the record could not settle, and the four accepted gaps are all `none`/`no-owner` — two are decisions the stakeholder or the architect has already taken, and two are toolkit defects `retro` is dispatched for without anybody filing.
- **Commands:**
  - `.claude/agile-skills/scripts/engagement-state EP-001` → `at-rest`; *"every child has stopped, nothing open is ours to act on, no request is open"*; rest reached 2026-09-11T11:57:12Z
  - `.claude/agile-skills/scripts/check-epic-signoff EP-001` → exit 0; *"carries the stakeholder's reply, names all 7 child item(s), and was filed after the engagement reached rest"*; DE8 satisfied by `EP-001/Q-001`
  - `.claude/agile-skills/scripts/lint-claims --context epic --changed-since main` → exit 0; scope *"every document under docs"*, *"every markdown file in the workspace"*; re-run after this execution's own edits
  - `.claude/agile-skills/scripts/lint-answers --context epic --changed-since main` → exit 0; 35 consumed human answers, 2 delegations spent
  - `.claude/agile-skills/scripts/lint-documents --rule engagement-state-is-restated --item EP-001 --context epic` → exit 0; 2 sections, both named in `## Sections restated at the ending`
  - `.claude/agile-skills/scripts/lint-documents --rule accepted-gaps-are-dispatchable --item EP-001` → exit 0; 4 gaps, each disposed
  - `.claude/agile-skills/scripts/validate-workspace` → exit 1, two errors, both the change-log window described under `workspace-valid`
  - `python3 -m unittest discover -s tests -t .` → exit 0, 310 tests, `OK`. Run on `main` for the record, **not** as evidence for `tests-pass-on-the-merge-result`, which is skipped here and which asks a different question (F-085)
  - `bin/envel` twice from clean stores under `ENVEL_FILE` — once to reproduce `groceries was 250.00 short at the start of 2026-09`, once as its falsifier, which printed `fun held 0.00 at the start of 2026-09`
- **Gates:**
  - `definition-of-done` → **pass** (`spec/dor-dod.md` §4 walked criterion by criterion in `review.md`'s table, nine rows with a result and evidence each, split by §4a: DE1, DE2, DE3, DE5, DE6 and DE4's first half were applied by the third review before the sign-off's `## Question` was written and are restated here with what this execution re-checked; DE4's restatement, DE7 and DE8 are applied here for the first time. No single overall verdict stands in for the walk.)
  - `engagement-state-is-restated` → **pass** (`lint-documents` exit 0; 2 sections enumerated by the gate, both named and both restated in `review.md`'s `## Sections restated at the ending`, written after the reply was read and the ending determined. The gate reads the naming; the truth of each restatement is my read and is recorded sentence by sentence.)
  - `accepted-gaps-are-dispatchable` → **pass** (`lint-documents` exit 0; 4 rows, each with a gap, an owner and a disposition. Every owner is `none` and every disposition `no-owner`, and each row says why that is the honest answer rather than a way of avoiding a filing — two are decisions already taken, two are toolkit defects `retro` is dispatched for on its own.)
  - `verification-postdates-the-code` → **skipped** (the contract's subject column: on an `epic` this gate has no subject — an epic has no branch and no `verify-report.md` of its own, so `check-verify-freshness` has nothing to compare. Each child's verification freshness was gated at that child's own close.)
  - `commits-reference-the-item` → **skipped** (the contract's subject column: on an `epic` there is no branch, so there is no range of commits not yet on the trunk to inspect. This epic's own record commits go on the trunk and carry `EP-001` in their subjects.)
  - `tests-pass-on-the-merge-result` → **skipped** (the contract's subject column: an ending merges nothing, so there is no merge result to test. The suite was run on `main` and is green at 310 tests, and that is recorded under `**Commands:**` as a fact about the trunk rather than borrowed here as a pass this gate did not earn — which is the mistake F-085 names.)
  - `workspace-valid` → **pass when run directly; the transition was taken with `--force`.** Read this in full, because the record must not overstate it.
    - Run on its own against the state this entry describes, `validate-workspace` reports **0 errors, 0 warnings** — reported in `**Result:**` from the run immediately after the transition.
    - It could not report that from *inside* the transition, and the deadlock is F-084's, which this engagement has now hit at least six times. This execution versioned two documents — `vision.md` to v10 and `overview.md` to v12 — and each change-log row says *review-close changed this at `2026-09-11T19:17:34Z` for `EP-001`*. `doc.changelog.no-execution` asks whether an execution of `review-close` on `EP-001` was running then, and the windows come from the journal: a window is *(the previous entry's stamp, this entry's stamp]*, and the entry that would close this one is the one this very transition writes. So both rows sit outside every window that exists and inside the one this entry creates.
    - `--resolving 'EP-001:open->done+journal'` was tried and does not cover it: it exits 1 with the same two errors. Nor does the rule's own *"a row on a closed item is history"* exemption, which would make both rows moot the instant `EP-001` is `done` — the check reads the item's status as it stands when the gate runs, not as the pending move leaves it.
    - The rows are honest and were not restamped. `spec/journal-and-history.md` §0 requires a change-log row's `when` to be read from a clock at the moment it is written, and `19:17:34Z` is when these were written — the first attempt at these rows carried an estimated `19:25:00Z` and `doc.changelog.timestamp.future` correctly refused it, which is the same rule working. Backdating them into an earlier `review-close` window to buy a green gate would record a time that is not true.
    - So the move was made with `--force`, which skips the gate run and records `[gates forced]` in the history reason for ever. **Every other gate in this contract was run by hand before that, and each passed**; the commands and their exit codes are under `**Commands:**` and can be re-run. This is the precedent `plan` set on `WI-0003` at 2026-09-11T06:26:47Z for the same deadlock, followed deliberately rather than reinvented.
    - It is also a finding, and it is now a **stronger** one than the previous turn recorded. There, the same rule failed inside `answer-questions`, whose transition is never gated, so the move proceeded and only the journal carried a `fail`. Here it falls on a skill's **completion** transition, where the gate blocks — so the rule does not merely misreport, it makes the legal move unreachable without an override. `retro` carries it.
  - `record-is-reconstructible` → **pass** (answered from the tracker, `docs/` and `git log` alone, without this session. *What was built and why*: seven items under `EP-001`, the goal in `item.md` and the product account in `vision.md`. *Which skill decided what*: every decision carries a journal entry and a history row, and the thirteen ADRs name their item. *What questions arose and how they resolved*: 35 question files, all `answered`, each with `## Consequences` naming files. *What verification found*: each child's `verify-report.md`, plus `BUG-0001`, which exists because a DE3 run at the first rest found a success measure failing in delivered code. The one thing a reader must be told rather than deduce — that three rests produced four `review.md` files and each replaced the last — is the table at the top of `review.md`, with a commit per predecessor.)
  - `claims-are-sourced` → **pass** (`lint-claims --context epic --changed-since main` exit 0. The scope, quoted from its own output: *"an ending has no diff of its own, so the scope is the whole document set rather than anything --changed-since could name; absolute claims: every document under .../docs; citations: every markdown file in the workspace"*. It was run again after the two restatements, so it examined this execution's own writing and not only its predecessors'.)
  - `cross-answer-consistency` → **pass** (`lint-answers --context epic --changed-since main` exit 0 over 35 consumed human answers and 2 delegations spent; rule 3 does not run at an ending, which the tool says itself. Every delegation ID spent in this engagement is named in `Q-009`'s `## Question`, which is the half rule 5 checks.)
  - `epic-sign-off` → **pass** (`check-epic-signoff EP-001` exit 0: the sign-off carries the stakeholder's reply, names all seven children, and was filed after rest was reached at 2026-09-11T11:57:12Z; DE8 satisfied by `EP-001/Q-001`, the elicitation `intake` filed at the beginning of the engagement.)
- **Artifacts:**
  - `tracker/items/EP-001/artifacts/review.md` — rewritten as the ending's review: what was examined, the DE6 delta audit with a falsifier per claim, the nine-row Definition of Done walk, both restated sections, the findings including the reading that was rejected, four accepted gaps and the verdict. It replaces the third review, and the table at the top names all three predecessors by commit.
  - `docs/product/vision.md` — v9 → v10, `updated-by: review-close`, `updated-for: EP-001`. Only `## Engagement state` changed, plus its change-log row.
  - `docs/architecture/overview.md` — v11 → v12, `updated-by: review-close`, `updated-for: EP-001`. Only `## Engagement state` changed, plus its change-log row.
  - `tracker/items/EP-001/item.md` — `outcome: delivered`, written by this transition's `--outcome` and not by an edit beforehand.
  - `tracker/board.md` — regenerated after the transition.
  - a commit of these files on `main`, referencing `EP-001`. No merge commit: an ending merges nothing, so `scripts/record-merge` has no subject here.
  - No bug filed, no question filed, no item filed. Nothing this execution found needed one.
- **Status:** `open` → `done`
- **Result:** The stakeholder accepted the engagement in their own words and `EP-001` ends at **E1 — delivered**, with `outcome: delivered`. Seven items were opened and all seven closed `delivered`: envelopes and income and a listing of what each holds, spending with an overspend refused, a month's summary, moving money between envelopes, correcting or removing an entry, listing what has been recorded, and a bug the pipeline found in its own delivered behaviour against the stakeholder's no-negative-envelope rule. All eight success measures were met against the running tool; all 35 questions are answered; both `## Engagement state` sections were restated from this ending. Four findings are recorded for `retro` to carry upstream, three of them about the toolkit rather than about this engagement. `validate-workspace` after the transition: 8 items, 15 documents, 0 errors, 0 warnings.

## 2026-09-11T19:33:51Z — retro v0.2.2 — process-analyst

- **Item:** EP-001
- **Trigger:** invoked on `EP-001` by `next` step 7 — `scripts/engagement-state EP-001` reported `ended`: *"the epic is 'done'; the engagement has ended and the retrospective has not been written"*. Nobody is waiting on this; the ending was recorded at 19:22:35Z and the stakeholder has already been told the work is finished.
- **Inputs read:** the same list as `artifacts/retro.md` `## What was read`, which is the authoritative one. In summary, and with the depth declared rather than implied:
  - all **8** items — every `item.md` and every `history.md` in full, the epic's `item.md` and `artifacts/review.md` in full
  - all **87** journal entries, at two depths: **49** in full, every bullet (`EP-001` 8, `WI-0001` 11, `WI-0002` 15, `BUG-0001` 9, `WI-0004`'s first 6); **38** with `Decisions`, `Cross-answer check`, `Questions raised`, `Status` and `Result` read in full and `Inputs read`, `Commands` and `Artifacts` not opened (`WI-0004`'s last 3, `WI-0003` 11, `WI-0005` 12, `WI-0006` 12). Their gate lines were read instead through a survey run across all eight items, which is where P-2 came from.
  - all **39** question files by frontmatter, and **14** in full
  - `docs/product/vision.md` v10 in full with its ten change-log rows; `docs/architecture/overview.md` v12's `## Engagement state` and all twelve change-log rows; `ADR-0002` and `ADR-0012` at the sentences named; the other eleven ADRs through their citations
  - the **28** distinct question citations under `docs/`, each followed to the answer it cites and read against the answers that came after it (step 5b)
  - `envel/summary.py` `left_column()` and `balance_line()`
  - the installed contracts for `answer-questions`, `review-close` and this skill, and `pipeline.yaml`'s status table
  - **as source:** `scripts/lib/record.py`, `scripts/validate-workspace`, `scripts/lint-documents`, `scripts/lint-answers`. Three findings are readings of these rather than of the engagement.
  - **Not available:** nothing. Every input this skill's contract lists was present.
- **Decisions:**
  - **Ten proposals: seven `toolkit-defect`, two `project-circumstance`, one `observation`.** The split is the whole of this skill's judgement and the hard calls are below.
  - **P-1 (the F-084 change-log deadlock) is the engagement's largest single finding and is `toolkit-defect` without difficulty.** Twelve forced transitions, every one of the seven `plan` executions among them, and the counterfactual names no subject matter: any skill whose own transition both journals the execution and bumps a document version reaches it. What made it worth writing up rather than repeating is the new half this turn added — on a **completion** transition the gate blocks, so the legal move is unreachable without `--force`, which is a stronger claim than the misreporting the previous turns recorded.
  - **P-2 was found by reading the set of gate lines, not any one entry, and it is the finding I would keep if I could keep one.** Each individual line looks like a typo. Across all eight items the pattern is mechanical: the transition rewrites the verdict token and leaves the author's sentence, so `not applicable` became `pass` on all ten item closes — against `review-close`'s own contract, which says never `passed` — and on one **rejection** `not run` became `pass` on `tests-pass-on-the-merge-result`, leaving the record asserting a merge result that does not exist. Classified `correctness of the record, high` rather than `structural` because the underlying gate runs did happen and are recoverable from the evidence sentences; what is wrong is the verdict written over them.
  - **P-5 was the hardest classification and it went to `toolkit-defect` on one specific ground.** Four executions stated the engagement's question count, three wrong, and the last wrote the wrong number into `docs/product/vision.md`. The tempting reading is carelessness, which would be nobody's finding. What makes it travel is that `dor-dod.md` R11 already exists — *a criterion names the artefacts it constrains rather than counting them, and a wanted count is measured first* — and applies only to acceptance criteria, so a count asserted in a review or in `docs/` is subject to no rule at all. The counterfactual is writable without naming envelopes: any engagement whose review states a count of its own artifacts. That is what moved it off `observation`.
  - **P-9 and P-10 are `project-circumstance` and I tried to write them as defects first.** P-9 — thirteen refinement rounds — cannot have its counterfactual written without this stakeholder: the rounds were caused by their answers opening questions nobody could have asked earlier, not by imprecision, and an engagement with settled requirements does not reach it. P-10 — a delivered feature withdrawn at sign-off — is the same: the four endings offered are the right four, the reply was an acceptance, and what was wanted was somewhere to record a parked part of a delivered scope, which `## Notes` supplied. Filing either as a toolkit defect would be the sympathy inflation this skill's own procedure names, and the counterfactual test is what exposed both.
  - **P-8 is an `observation` and deliberately proposes nothing.** `implement` and `verify` filed zero of 39 questions, which is a striking fact about the protocol's main intended users. One engagement cannot tell *the plans were complete* from *the escalation bar sits too high mid-build*, and both readings fit a record that shows the test being applied rather than skipped. Proposing a change from one engagement's data would be the inflation P-9 and P-10 were kept out for.
  - **I found a live defect in a document and did not repair it.** `docs/product/vision.md` v10 `## Engagement state` says *"Thirty-five questions were filed"* and there are 39. This skill is read-only over the engagement it audits, so it is reported in the observation and in P-5, stated as needing a later execution through a new request, and left exactly as it is. The tell that this was the right call is that the sentence is in a section only an **ending** may write, and this engagement's ending has been recorded.
  - **The DE6 sweep was not repeated and no product claim was re-audited.** Whether the software is any good was `verify`'s question and `review-close`'s, and it was answered before the ending. The one code read this execution made — `left_column()` and `balance_line()` — was to check a claim written into `docs/` **after** the last full audit, which is a fact about the record rather than about the product.
  - **What I could not find is stated rather than implied.** Two things this reading could not settle: whether the 38 entries read at the second depth hide anything in their `Commands` bullets that would change a finding, and whether P-8's zero is a property of this engagement or of the protocol. Both are in the report.
- **Cross-answer check:** `none — this execution consumed no human answer.` `retro` reads a closed record and answers nothing; every human answer in this engagement was consumed by `answer-questions` and carries its own check, and this report edits none of them. The one cross-answer question this skill **is** positioned to ask — step 5b, whether a sentence in `docs/` stands on an answer a later answer overtook — was asked across all 28 question citations and is written up in `## Positive record`: exactly one answer was overtaken, `EP-001/Q-003` by `EP-001/Q-009`, and the sentence carrying it was not rewritten.
- **Questions raised:** none. `retro` files no question, no bug and no item — it is read-only over the engagement it audits, and the two things it found that warrant work (the false count in `docs/product/vision.md`, and the seven toolkit defects) are recorded as proposals for triage rather than filed.
- **Commands:**
  - `.claude/agile-skills/scripts/engagement-state EP-001` → exit 0, `ended` before the report, `closed` after it
  - `.claude/agile-skills/scripts/lint-retro EP-001` → exit 1 first with 14 citation errors, exit 0 after repair
  - `.claude/agile-skills/scripts/lint-retro EP-001 --require-scope` → exit 0
  - `.claude/agile-skills/scripts/validate-workspace` → exit 1 once on a malformed marker of my own, exit 0 after; 8 items, 15 documents
  - `ls tracker/items/*/questions/Q-*.md | wc -l` → 39
  - `grep -h "^from-skill:" tracker/items/*/questions/Q-*.md | sort | uniq -c` → 6 intake, 1 plan, 27 refine, 5 review-close
  - `grep -h "^kind:" tracker/items/*/questions/Q-*.md | sort | uniq -c` → 1 elicitation, 1 sign-off
  - `grep -h "gates forced" tracker/items/*/history.md | wc -l` → 12
  - `grep -rn "engagement-state-is-restated. → " tracker/items/*/journal.md` → 14 lines, every one `**pass**`
  - `grep -rlF "/Q-0" docs/ | wc -l` → 13
  - `grep -n "GAP_NONE_RE" .claude/agile-skills/scripts/lint-documents` → the prefix match behind P-4
  - `.claude/agile-skills/scripts/lint-answers --item EP-001` → exit 0, *checked 35 consumed human answer(s)* — the number P-5 is about
- **Gates:**
  - `engagement-has-ended` → **pass** (`scripts/engagement-state EP-001` reported `ended` before this execution wrote anything — *"the epic is 'done'; the engagement has ended and the retrospective has not been written"* — which is the one verdict this skill may run on. It reports `closed` now, and the change is this report existing.)
  - `retro-report-is-well-formed` → **pass** (`scripts/lint-retro EP-001` exit 0. The first run exited 1 with 14 citation errors, every one a malformed marker of mine rather than a finding that needed softening: four `toolkit:` citations missing a section or carrying a `;` inside the quote, three `run:` citations recording a command with no outcome, four `path:line` citations pointing into `.claude`, which the record walk prunes, and four line numbers taken from a working extract rather than from the file. Each was repaired to a form that resolves; no observation and no proposal was reworded to get past the gate.)
  - `scope-was-not-degenerate` → **pass** (`scripts/lint-retro EP-001 --require-scope` exit 0. The declared scope is 8 items, 87 journal entries and 10 proposals, cross-checked against a workspace that holds 8 items and 87 entries. The two reading depths are declared in `## What was read` and in `**Inputs read:**` above rather than averaged into a single claim, because a report that overstates its own scope is what this gate exists against.)
  - `the-record-was-not-touched` → **pass** (this execution wrote exactly two files: `tracker/items/EP-001/artifacts/retro.md` and this entry on `tracker/items/EP-001/journal.md`. No item, no question, no document under `docs/`, no bug, no status. The journal body was piped from `/tmp/retro-body.md`, outside the workspace, so no scratch file was created inside it — and the working extract of the last three journals was likewise written to `/tmp`. The false sentence found in `docs/product/vision.md` was deliberately left: repairing the record it audits is the one thing this skill may not do.)
  - `workspace-valid` → **pass** (`scripts/validate-workspace` exit 0, 8 items, 15 documents, 0 errors, 0 warnings — run after the report was written. One interim failure is worth recording because it is the same class as P-3: a `run:` citation of mine embedded a `grep` pattern containing `]`, which ends a citation marker, so the marker parsed as unresolved. The citation was rewritten to a bracket-free command rather than dropped.)
- **Artifacts:**
  - `tracker/items/EP-001/artifacts/retro.md` — created
- **Status:** `done` → `done` (unchanged — `retro` writes no history row and changes no status)
- **Result:** `EP-001`'s retrospective is written. Ten observations and six entries in the positive record, from 8 items, 87 journal entries, 39 questions and 28 question citations under `docs/`. Ten proposals go up for triage: seven `toolkit-defect` — the change-log/journal-window deadlock that forced twelve transitions and now blocks a completion move; the gate-verdict rewriter that overwrote `not applicable` with `pass` on every item close and `not run` with `pass` on a rejection; `path:line` citations, repaired four times with the first repair predicting the rest in writing; a gaps gate that drops any row beginning "Nothing"; a count of the record's own artifacts asserted rather than measured, which put a false sentence into `docs/product/vision.md`; no rule for an artifact written twice on one item; and `answer-questions`' step 6a being unexecutable after a close. Two are `project-circumstance` and one is an `observation` that proposes nothing. What held is recorded too, and the largest of it is that `dor-dod.md` §4a's ordering caught a false success measure and four unsupported citations **before** the stakeholder was asked, so each became work instead of a retracted acceptance. The engagement is now `closed`.
