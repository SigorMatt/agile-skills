# Probe script — iteration 5b, `droll`

Source: `meta/harness/PROJECT-QUEUE.md`, iteration 5b. Persona: `ghosting-founder`.

One planted behavior: the ghosting itself, defined in the persona's two phases. This
script supplies the idea and the phase-1 answers.

## 1. The idea, as you state it

Write this into `IDEA.md` on your opening turn, in this voice:

> A little command-line dice roller: I type something like `3d6+2`, it rolls and shows
> the result and the breakdown. It should remember the rolls from the current session so
> I can see a history. Python.

Nothing else goes in `IDEA.md`.

## 2. Phase-1 answers (the one round you answer)

Answer the FIRST round of refinement questions addressed to you, in persona, using
these; anything not covered, answer briefly and reasonably `[ORGANIC]`:

- **Who uses it:** you and your gaming group on game night.
- **Dice specs:** the usual — `d20`, `2d8+1`, that kind of thing. If they ask about
  weird edge cases: "whatever's standard, don't overthink it."
- **History:** current session only is fine. If they ask about persisting across
  sessions: "nice but not needed."
- **Output:** total plus the individual dice. Plain text.

After this round: phase 2. You are gone. Every subsequent turn follows the persona's
phase-2 rules — read, withhold with the `[PLANTED: ghosting]` tag, answer nothing,
forever.

## 3. What this probe is for

Ending E4 (`abandoned`) and its whole mechanism: the silence threshold, the abandonment
declaration by review-close, the termination statement naming delivered and orphaned
children, and the driver stopping on the declared ending rather than diagnosing a stall.
Your silence is the test fixture. Note nothing, manufacture nothing, return never.
